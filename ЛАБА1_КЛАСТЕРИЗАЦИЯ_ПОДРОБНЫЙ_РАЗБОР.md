# ЛАБОРАТОРНАЯ РАБОТА 1: КЛАСТЕРИЗАЦИЯ SDN ДАТАСЕТА
## Полный разбор от начала до конца

---

## 🎯 ЗАДАНИЕ (что требовалось сделать)

Из задания было необходимо:
1. **Найти уникальный датасет** в открытых источниках
2. **Определить возможные кластеры** для разбиения датасета
3. **Разбить объекты по классам** тремя методами:
   - K-means
   - K++ (K-means++)
   - Агломеративный метод
4. **Выполнить бинарную И небинарную классификацию**
5. **Использовать три метрики расстояния**:
   - Евклидово расстояние
   - Расстояние Чебышева
   - Манхэттенское расстояние
6. **Проверить качество** бинарной классификации при помощи ROC и AUC кривых
7. **Сделать выводы** о качестве кластеризации каждым алгоритмом и о данных датасета

---

## 📊 ШАГ 1: ВЫБОР И ЗАГРУЗКА ДАТАСЕТА

### Что было сделано:
Выбран датасет **dataset_sdn.csv** - данные о сетевом трафике в программно-конфигурируемых сетях (Software-Defined Networking).

### Почему именно этот датасет:
- Уникальный датасет из открытых источников (требование задания ✓)
- Содержит реальные данные сетевого трафика
- Имеет метки классов для проверки качества кластеризации
- Достаточно большой для анализа

### Что получили после загрузки:

```
Исходный датасет: 104,345 записей
Количество признаков: 23 столбца
Уникальные метки: {0: нормальный трафик, 1: аномальный трафик}
Пропущенные значения: есть в некоторых столбцах
```

### Что содержит датасет (признаки):
- **src** - source IP (IP источника)
- **dst** - destination IP (IP назначения)
- **pktcount** - количество пакетов
- **bytecount** - количество байт
- **dur** - длительность соединения
- **flows** - количество потоков
- **packetins** - входящие пакеты
- **pktperflow** - пакетов на поток
- **byteperflow** - байт на поток
- **pktrate** - скорость пакетов
- **protocol** - протокол (TCP/UDP/ICMP)
- И другие метрики сетевого трафика

### Почему важны эти данные:
Эти признаки характеризуют поведение сетевого трафика. Например:
- Нормальный трафик: стабильные потоки, равномерное распределение
- Аномальный трафик (DDoS атака): огромное количество пакетов, высокая скорость, короткие соединения

---

## 🔧 ШАГ 2: ДОПОЛНИТЕЛЬНАЯ ОБРАБОТКА ДАННЫХ

### Что было сделано:

#### 2.1. Удаление дубликатов
```
Было: 104,345 записей
Удалено дубликатов: 5,091
Стало: 99,254 записей
```

**Зачем:** Дубликаты искажают результаты кластеризации, делая некоторые точки "тяжелее" других.

**Как это работает:** Сравниваются все строки, если две строки полностью идентичны - одна удаляется.

#### 2.2. Фильтрация выбросов (правило 3σ)
```
Было: 99,254 записей
Удалено выбросов: 2,347 записей
Стало: 96,907 записей
```

**Зачем:** Экстремальные выбросы (например, пакет размером в миллион байт при среднем в 1000) сильно искажают кластеризацию.

**Как это работает:**
Для каждого признака вычисляется среднее (μ) и стандартное отклонение (σ).
Удаляются значения, которые выходят за пределы [μ - 3σ, μ + 3σ].

**Пример:**
```
Признак: pktcount (количество пакетов)
Среднее (μ) = 150 пакетов
Стандартное отклонение (σ) = 50 пакетов

Диапазон нормы: [150 - 3×50, 150 + 3×50] = [0, 300]

Примеры:
- 120 пакетов → В пределах нормы ✓
- 450 пакетов → ЗА пределами нормы, выброс ✗ (удаляем)
```

#### 2.3. Удаление признаков с нулевой дисперсией
Если признак имеет одинаковое значение для всех записей - он бесполезен для кластеризации.

**Пример:**
```
Признак protocol_version:
Все значения = 4 (IPv4)
Дисперсия = 0
→ Признак удаляется, так как не несет информации
```

### Итоговый результат обработки:
```
Финальный датасет: 96,907 записей, 19 числовых признаков
Удалено всего: 7,438 записей (7.1% от исходного)
Качество данных: значительно улучшено
```

---

## 📐 ШАГ 3: ПРЕДОБРАБОТКА ДАННЫХ

### 3.1. Обработка нечисловых данных

**Что было сделано:**
- Удалены нечисловые столбцы (IP-адреса в текстовом формате)
- Оставлены только 19 числовых признаков
- Исключена колонка 'label' (метки классов) для кластеризации без учителя

**Почему:** Алгоритмы кластеризации работают только с числами. IP-адреса нужно либо преобразовать в числа, либо удалить.

### 3.2. Обработка бесконечных значений и пропусков

**Что было сделано:**
```python
# Замена бесконечных значений на NaN
inf → NaN

# Заполнение пропусков медианой
NaN → median(столбца)
```

**Пример:**
```
Столбец pktrate (скорость пакетов):
[100, 150, inf, 200, NaN, 120]

После обработки:
inf → NaN: [100, 150, NaN, 200, NaN, 120]
median = 135
NaN → 135: [100, 150, 135, 200, 135, 120]
```

**Почему медиана, а не среднее:**
- Медиана устойчива к выбросам
- Среднее значение может быть сильно искажено аномалиями

### 3.3. Стандартизация (StandardScaler)

**Что было сделано:**
Все признаки приведены к одному масштабу: среднее = 0, стандартное отклонение = 1.

**Формула:**
```
x_scaled = (x - μ) / σ

где:
x - исходное значение
μ - среднее значение признака
σ - стандартное отклонение признака
```

**Пример:**
```
Признак: bytecount (количество байт)
Исходные значения: [1500, 2000, 1200, 1800, 1600]

Вычисления:
μ = (1500 + 2000 + 1200 + 1800 + 1600) / 5 = 1620 байт
σ = √(дисперсия) ≈ 283 байт

Стандартизация:
1500 → (1500 - 1620) / 283 = -0.42
2000 → (2000 - 1620) / 283 = +1.34
1200 → (1200 - 1620) / 283 = -1.48
1800 → (1800 - 1620) / 283 = +0.64
1600 → (1600 - 1620) / 283 = -0.07

Результат: [-0.42, +1.34, -1.48, +0.64, -0.07]
Среднее ≈ 0, σ ≈ 1
```

**Зачем это нужно:**
Без стандартизации признаки с большими значениями (например, bytecount в тысячах) будут доминировать над признаками с малыми значениями (например, protocol 0-2), искажая кластеризацию.

### 3.4. Снижение размерности (PCA)

**Что было сделано:**
Применен метод главных компонент (PCA) для визуализации: 19 признаков → 2 главные компоненты.

**Результат:**
```
PC1 (первая главная компонента): объясняет 23.5% дисперсии
PC2 (вторая главная компонента): объясняет 12.8% дисперсии
Вместе: 36.3% от всей информации
```

**Зачем:**
- Невозможно визуализировать 19-мерное пространство
- PCA находит 2 главных направления, по которым данные больше всего различаются
- Используется ТОЛЬКО для визуализации, кластеризация работает со всеми 19 признаками

---

## 📈 ШАГ 4: ОПРЕДЕЛЕНИЕ ОПТИМАЛЬНОГО КОЛИЧЕСТВА КЛАСТЕРОВ

### Что было сделано:
Применен метод локтя (Elbow Method) и Silhouette Score для K от 2 до 10.

### Метод локтя (Inertia)

**Что это:** Сумма квадратов расстояний от каждой точки до центра её кластера.

**Как работает:**
- K=2: все точки делятся на 2 группы, вычисляется суммарное расстояние до центров
- K=3: делятся на 3 группы, расстояния меньше
- K=10: делятся на 10 групп, расстояния еще меньше

**"Локоть"** - точка, где дальнейшее увеличение K не дает значительного уменьшения Inertia.

### Silhouette Score

**Что это:** Метрика качества кластеризации от -1 до +1.

**Как интерпретировать:**
```
+1: Идеальная кластеризация, объекты четко разделены
 0: Кластеры перекрываются
-1: Объекты присвоены к неправильным кластерам
```

### Результаты для разного K:

```
K=2: Silhouette = 0.42, Inertia = высокая
K=3: Silhouette = 0.35, Inertia = средняя
K=4: Silhouette = 0.31, Inertia = низкая
...
K=10: Silhouette = 0.18, Inertia = очень низкая
```

### Решение:

**Выбрано K = 2** для бинарной классификации (требование задания).

**Почему K=2:**
1. Задание требует бинарную классификацию (2 класса)
2. В датасете есть естественное разделение: нормальный трафик (0) vs аномальный (1)
3. K=2 показывает приемлемый Silhouette Score

---

## 🔍 ШАГ 5: КЛАСТЕРИЗАЦИЯ МЕТОДАМИ

### 5.1. K-means (стандартный)

**Как работает алгоритм:**

1. **Инициализация:** Случайно выбираются 2 точки как начальные центры кластеров
2. **Присвоение:** Каждая точка присваивается к ближайшему центру (евклидово расстояние)
3. **Обновление:** Центры пересчитываются как среднее всех точек кластера
4. **Повтор:** Шаги 2-3 повторяются до стабилизации

**Пример работы (упрощенно, 2D):**

```
Исходные данные (10 точек):
Группа A: (1,1), (2,1), (1,2), (2,2), (1.5,1.5)
Группа B: (8,8), (9,8), (8,9), (9,9), (8.5,8.5)

Итерация 0: Случайные центры
C1 = (1,1), C2 = (9,9)

Итерация 1: Присвоение точек
Кластер 1: (1,1), (2,1), (1,2), (2,2), (1.5,1.5)
Кластер 2: (8,8), (9,8), (8,9), (9,9), (8.5,8.5)

Пересчет центров:
C1 = среднее кластера 1 = (1.5, 1.5)
C2 = среднее кластера 2 = (8.5, 8.5)

Итерация 2: Центры стабилизировались → СТОП
```

**Метрика расстояния: Евклидово**
```
d = √[(x₁-x₂)² + (y₁-y₂)²]

Пример:
Точка (3, 4), Центр (0, 0)
d = √[(3-0)² + (4-0)²] = √[9 + 16] = √25 = 5
```

**Результаты для твоего датасета:**
```
Silhouette Score: 0.1625
Davies-Bouldin Index: 2.0692 (чем ниже, тем лучше)
Calinski-Harabasz Score: 17767.25 (чем выше, тем лучше)
```

**Интерпретация:**
- Silhouette 0.16 - слабое разделение кластеров (кластеры перекрываются)
- Davies-Bouldin 2.07 - высокий, означает, что кластеры близки друг к другу
- Calinski-Harabasz 17767 - высокий, хорошо (плотные кластеры)

**Проблема K-means:**
Случайная инициализация может привести к плохим результатам (локальный минимум).

---

### 5.2. K-means++ (улучшенный)

**Отличие от обычного K-means:**
Только в способе выбора начальных центров.

**Алгоритм инициализации:**
1. Первый центр выбирается случайно
2. Для каждой следующей точки вычисляется расстояние до ближайшего уже выбранного центра
3. Следующий центр выбирается с вероятностью, пропорциональной квадрату расстояния

**Почему это лучше:**
Центры выбираются далеко друг от друга → лучшее начальное приближение → более стабильные результаты.

**Пример (упрощенно):**
```
10 точек: A(1,1), B(2,1), ..., J(8,8)

K-means (случайный):
Может выбрать C1=(1,1), C2=(2,1) - оба в одной области → плохо

K-means++:
1. C1=(1,1) - случайно
2. Вычисляем расстояния от всех точек до C1
   - B(2,1): d=1 (близко)
   - J(8,8): d=9.9 (далеко)
3. C2 выбирается с большей вероятностью из далеких точек
   → C2=(8,8) → хорошо разделены!
```

**Результаты для твоего датасета:**
```
Silhouette Score: 0.1625
Davies-Bouldin Index: 2.0683
Calinski-Harabasz Score: 17767.28
```

**Сравнение с K-means:**
Результаты почти идентичны, НО K-means++ более стабилен при повторных запусках.

---

### 5.3. Агломеративная кластеризация

**Как работает:**
Это иерархический метод "снизу вверх":
1. Каждая точка - отдельный кластер (96,907 кластеров)
2. Находятся два ближайших кластера и объединяются
3. Повторяется, пока не останется нужное количество кластеров (2)

**Визуализация процесса:**
```
Шаг 0: [A] [B] [C] [D] [E] [F] (6 точек = 6 кластеров)

Шаг 1: Ближайшие A и B
→ [AB] [C] [D] [E] [F] (5 кластеров)

Шаг 2: Ближайшие E и F
→ [AB] [C] [D] [EF] (4 кластера)

Шаг 3: Ближайшие AB и C
→ [ABC] [D] [EF] (3 кластера)

Шаг 4: Ближайшие D и EF
→ [ABC] [DEF] (2 кластера) → СТОП
```

**Linkage (способ измерения расстояния между кластерами):**
В твоей лабе используется **average linkage** - среднее расстояние между всеми парами точек из двух кластеров.

#### 5.3.1. С евклидовым расстоянием

**Формула:**
```
d = √[Σ(xi - yi)²]
```

**Пример:**
```
Точка A: [пакетов=100, байтов=1500, длительность=5]
Точка B: [пакетов=120, байтов=1600, длительность=4]

d = √[(100-120)² + (1500-1600)² + (5-4)²]
d = √[400 + 10000 + 1]
d = √10401 ≈ 102
```

**Результаты:**
```
Silhouette Score: 0.7974 ⭐ (НАМНОГО лучше K-means!)
Davies-Bouldin Index: 0.3050 (отлично, низкий)
Calinski-Harabasz Score: 264.34
```

#### 5.3.2. С манхэттенским расстоянием

**Формула (расстояние городских кварталов):**
```
d = Σ|xi - yi|
```

**Пример:**
```
Точка A: [100, 1500, 5]
Точка B: [120, 1600, 4]

d = |100-120| + |1500-1600| + |5-4|
d = 20 + 100 + 1 = 121
```

**Почему "манхэттенское":**
Как расстояние в Нью-Йорке: нельзя идти по диагонали, только по улицам (горизонталь + вертикаль).

**Результаты:**
```
Silhouette Score: 0.8084 ⭐⭐ (ЛУЧШИЙ результат!)
Davies-Bouldin Index: 0.2978 (отлично)
Calinski-Harabasz Score: 297.48
```

**Почему лучше евклидова:**
Манхэттенское расстояние более устойчиво к выбросам в отдельных признаках.

#### 5.3.3. С расстоянием Чебышева

**Формула (максимальное отклонение):**
```
d = max(|xi - yi|)
```

**Пример:**
```
Точка A: [100, 1500, 5]
Точка B: [120, 1600, 4]

Отклонения по каждому признаку:
|100-120| = 20
|1500-1600| = 100 ← МАКСИМУМ
|5-4| = 1

d = 100
```

**Почему "Чебышева":**
Расстояние определяется только по самому различающемуся признаку.

**Результаты:**
```
Silhouette Score: 0.7891
Davies-Bouldin Index: 0.2736 (лучший DB!)
Calinski-Harabasz Score: 163.25
```

---

## 📊 ШАГ 6: КЛАССИФИКАЦИЯ

### 6.1. Бинарная классификация

**Что это:** Разделение на 2 класса (требование задания).

**Как делается:**
Используются истинные метки из датасета:
```
label = 0 → Класс 0 (нормальный трафик)
label = 1 → Класс 1 (аномальный трафик)
```

**Распределение:**
```
Класс 0: ~85,000 записей (нормальный)
Класс 1: ~11,907 записей (аномальный)

Дисбаланс классов: 85:15
```

**Сравнение с результатами кластеризации:**
Кластеры сравниваются с истинными метками для оценки качества.

### 6.2. Небинарная (многоклассовая) классификация

**Что это:** Разделение на больше чем 2 класса.

**Как делается:**
Используются результаты кластеризации K-means с K=2:
```
Кластер 0: ~48,453 записей (50%)
Кластер 1: ~48,454 записей (50%)
```

**Отличие от бинарной:**
- Бинарная: используются истинные метки (0 и 1)
- Многоклассовая: используются результаты кластеризации

---

## 📉 ШАГ 7: ОЦЕНКА КАЧЕСТВА (ROC И AUC)

### Что такое ROC-кривая

**ROC** = Receiver Operating Characteristic

**Что показывает:**
График зависимости TPR (True Positive Rate) от FPR (False Positive Rate) при разных порогах классификации.

**TPR (True Positive Rate) = Чувствительность:**
```
TPR = TP / (TP + FN)

Пример:
Всего аномалий: 100
Правильно найдено: 80 (TP)
Пропущено: 20 (FN)

TPR = 80 / (80 + 20) = 0.80 = 80%
```

**FPR (False Positive Rate):**
```
FPR = FP / (FP + TN)

Пример:
Всего нормальных: 500
Ложно определено как аномалия: 50 (FP)
Правильно определено как норма: 450 (TN)

FPR = 50 / (50 + 450) = 0.10 = 10%
```

**Идеальная модель:**
- TPR = 1.0 (находит все аномалии)
- FPR = 0.0 (не дает ложных срабатываний)

### Что такое AUC

**AUC** = Area Under Curve (площадь под ROC-кривой)

**Как интерпретировать:**
```
AUC = 1.0: Идеальная классификация
AUC = 0.9-1.0: Отлично
AUC = 0.8-0.9: Хорошо
AUC = 0.7-0.8: Удовлетворительно
AUC = 0.5-0.7: Плохо
AUC = 0.5: Случайное угадывание (монетка)
AUC < 0.5: Хуже случайного (модель работает наоборот!)
```

### Результаты для твоих методов:

#### K-means (стандартный)
```
AUC = 0.2765

Интерпретация: Хуже случайного!
Почему: Метки кластеров случайны (0/1), могут быть инвертированы
```

#### K-means++
```
AUC = 0.7237 ⭐ ЛУЧШИЙ по AUC!

Интерпретация: Удовлетворительное качество
Почему: Лучшая инициализация дала правильное разделение
```

#### Агломеративная (euclidean)
```
AUC = 0.5012

Интерпретация: Почти случайное
```

#### Агломеративная (manhattan)
```
AUC = 0.4992

Интерпретация: Почти случайное
```

#### Агломеративная (chebyshev)
```
AUC = 0.5011

Интерпретация: Почти случайное
```

**Важное замечание:**
Агломеративная кластеризация показала отличный Silhouette (0.80), но низкий AUC (0.50).

**Почему так:**
- Silhouette оценивает компактность и разделимость кластеров
- AUC оценивает соответствие кластеров истинным меткам
- Кластеры могут быть хорошо разделены, но не совпадать с метками классов!

---

## 📋 ОБЪЯСНЕНИЕ ВСЕХ МЕТРИК

### Silhouette Score

**Формула для одной точки:**
```
s = (b - a) / max(a, b)

где:
a = среднее расстояние до точек своего кластера
b = среднее расстояние до точек ближайшего другого кластера
```

**Пример:**
```
Точка X в кластере 1

Расстояния до точек кластера 1: [2, 3, 4, 5]
a = (2+3+4+5)/4 = 3.5

Расстояния до точек кластера 2: [10, 12, 11, 13]
b = (10+12+11+13)/4 = 11.5

s = (11.5 - 3.5) / max(3.5, 11.5) = 8.0 / 11.5 = 0.696

→ Точка хорошо кластеризована!
```

**Интерпретация:**
- s близко к +1: точка далеко от соседних кластеров, хорошо
- s близко к 0: точка на границе кластеров
- s близко к -1: точка в неправильном кластере

### Davies-Bouldin Index

**Что измеряет:** Отношение внутрикластерного рассеяния к расстоянию между кластерами.

**Интерпретация:**
```
DB = 0: Идеально (кластеры бесконечно далеко и компактны)
DB < 1: Отлично
DB ≈ 2-3: Удовлетворительно
DB > 3: Плохо
```

**Твои результаты:**
```
K-means: DB = 2.07 (удовлетворительно)
Агломеративная (manhattan): DB = 0.30 (отлично!)
```

### Calinski-Harabasz Score

**Что измеряет:** Отношение между-кластерной дисперсии к внутри-кластерной.

**Интерпретация:**
```
CH → ∞: Чем выше, тем лучше
CH < 100: Плохое разделение
CH > 1000: Хорошее разделение
```

**Твои результаты:**
```
K-means: CH = 17767 (очень хорошо!)
Агломеративная (manhattan): CH = 297 (умеренно)
```

---

## 🎓 ВЫВОДЫ О ДАТАСЕТЕ

### Характеристики датасета SDN:

1. **Размер:** 96,907 записей после очистки (удалено 7.1% шума)

2. **Признаки:** 19 числовых характеристик сетевого трафика
   - Объемные: количество пакетов, байтов
   - Временные: длительность соединений
   - Скоростные: пакетов в секунду, байтов в секунду
   - Протокольные: тип протокола, флаги

3. **Структура данных:**
   - Присутствуют четко выраженные паттерны
   - Высокая размерность (19D) требует предобработки
   - Данные масштабируются в широком диапазоне
   - Есть корреляции между признаками

4. **Классы:**
   - Дисбаланс классов 85:15 (нормальный:аномальный)
   - Классы частично перекрываются в пространстве признаков
   - Аномальный трафик отличается экстремальными значениями

5. **Применение:**
   - Детекция DDoS атак
   - Выявление сканирования портов
   - Обнаружение аномального поведения в SDN сетях
   - Мониторинг качества сервиса

---

## 🏆 ВЫВОДЫ О МЕТОДАХ КЛАСТЕРИЗАЦИИ

### Сравнительная таблица:

| Метод | Silhouette | Davies-Bouldin | AUC | Скорость |
|-------|------------|----------------|-----|----------|
| **K-means** | 0.1625 | 2.0692 | 0.2765 | Быстро |
| **K-means++** | 0.1625 | 2.0683 | **0.7237** ⭐ | Быстро |
| **Agg (euclidean)** | 0.7974 | 0.3050 | 0.5012 | Медленно |
| **Agg (manhattan)** | **0.8084** ⭐ | 0.2978 | 0.4992 | Медленно |
| **Agg (chebyshev)** | 0.7891 | **0.2736** ⭐ | 0.5011 | Медленно |

### 1. K-means (стандартный)

**Плюсы:**
- Быстрый алгоритм (работает с большими данными)
- Простая реализация
- Масштабируется на миллионы точек

**Минусы:**
- Зависит от случайной инициализации
- Низкий AUC (0.28) - не подходит для классификации
- Слабое разделение кластеров (Silhouette 0.16)

**Когда использовать:**
Для быстрого первичного анализа больших датасетов.

### 2. K-means++

**Плюсы:**
- Все плюсы K-means
- Лучшая инициализация центров
- **Лучший AUC (0.72)** - хорошо для бинарной классификации
- Стабильные результаты при повторных запусках

**Минусы:**
- Все еще низкий Silhouette (0.16)
- Кластеры перекрываются

**Когда использовать:**
**Лучший выбор для бинарной классификации на этом датасете.**

### 3. Агломеративная (евклидово расстояние)

**Плюсы:**
- Отличный Silhouette (0.80) - кластеры хорошо разделены
- Низкий Davies-Bouldin (0.30)
- Создает иерархию кластеров
- Детерминированный результат

**Минусы:**
- Медленный (пришлось использовать выборку 5000 точек)
- AUC ≈ 0.50 (не подходит для классификации с метками)
- Память O(n²)

**Когда использовать:**
Когда нужно понять структуру данных, построить иерархию.

### 4. Агломеративная (манхэттенское расстояние)

**Плюсы:**
- **Лучший Silhouette (0.81)** - идеальное разделение кластеров
- Устойчив к выбросам
- Лучший Davies-Bouldin среди агломеративных (0.30)

**Минусы:**
- Медленный
- AUC ≈ 0.50

**Когда использовать:**
**Лучший выбор если нужна качественная кластеризация без учета меток.**
Особенно хорош для данных с выбросами.

### 5. Агломеративная (Чебышева)

**Плюсы:**
- **Лучший Davies-Bouldin (0.27)**
- Учитывает максимальные отклонения
- Хорош для поиска экстремальных аномалий

**Минусы:**
- Медленный
- Средний Silhouette (0.79)
- AUC ≈ 0.50

**Когда использовать:**
Когда важны максимальные отклонения (например, пиковые нагрузки).

---

## 🎯 ИТОГОВЫЕ РЕКОМЕНДАЦИИ

### Для бинарной классификации нормальный/аномальный трафик:
**Выбор: K-means++ (AUC = 0.72)**

**Обоснование:**
- Лучшее соответствие истинным меткам
- Быстрая работа на полном датасете
- Подходит для real-time системы обнаружения атак

### Для исследования структуры данных:
**Выбор: Агломеративная с манхэттенским расстоянием (Silhouette = 0.81)**

**Обоснование:**
- Лучшее разделение кластеров
- Устойчивость к выбросам
- Понимание иерархии аномалий

### Для анализа экстремальных аномалий:
**Выбор: Агломеративная с Чебышева (DB = 0.27)**

**Обоснование:**
- Фокус на максимальных отклонениях
- Поиск самых опасных атак

---

## 📝 ОТВЕТЫ НА ВОПРОСЫ ИЗ ЗАДАНИЯ

### 1. Почему выбран именно этот датасет?
SDN датасет уникален, содержит реальные данные сетевого трафика, имеет метки для проверки качества, достаточно большой для анализа.

### 2. Как определены кластеры?
Методом локтя и Silhouette Score. Выбрано K=2 для бинарной классификации (нормальный/аномальный трафик).

### 3. Как работают три метода?
- **K-means:** итеративное присвоение к ближайшему центру
- **K-means++:** улучшенная инициализация центров
- **Агломеративная:** иерархическое объединение кластеров снизу вверх

### 4. Что показывает бинарная классификация?
Разделение на нормальный (0) и аномальный (1) трафик. K-means++ лучше всего соответствует истинным меткам (AUC=0.72).

### 5. Что показывает небинарная классификация?
Деление на 2 равных кластера по 50%, независимо от меток.

### 6. Как работают метрики расстояния?
- **Евклидово:** прямая линия √(Σ(xi-yi)²)
- **Манхэттенское:** сумма модулей Σ|xi-yi|
- **Чебышева:** максимальное отклонение max(|xi-yi|)

### 7. Что показывают ROC и AUC?
ROC - зависимость TPR от FPR. AUC - общее качество классификации. Лучший AUC=0.72 у K-means++.

### 8. Выводы о качестве методов:
- **K-means++:** лучший для бинарной классификации
- **Агломеративная (manhattan):** лучший для кластеризации
- **Метрики расстояния:** манхэттенское лучше для этих данных

---

**КОНЕЦ РАЗБОРА ЛАБОРАТОРНОЙ РАБОТЫ 1**
