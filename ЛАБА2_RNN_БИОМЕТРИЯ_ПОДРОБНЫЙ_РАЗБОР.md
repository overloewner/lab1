# ЛАБОРАТОРНАЯ РАБОТА 2: RNN ДЛЯ БИОМЕТРИЧЕСКОЙ ИДЕНТИФИКАЦИИ
## Полный разбор от начала до конца

---

## 🎯 ЗАДАНИЕ (чек-лист лабы 2)

Из задания было необходимо:
1. **Dataset по биометрии** (поведенческая/статическая) → если новый, то нормализация
2. **Разработка архитектуры RNN**
3. **Обучение** с алгоритмами → процент тестирования, процент обучения, график переобучения
4. **Метрики:**
   - acc, rec, f-мера, TPR, FPR, TP, TN, FP, FN, MSE, MSA
   - ROC-кривая
   - Сравнение TPR и FPR с ROC-кривой
   - AUC
5. **Тесты на реальных примерах**
6. **Выводы**
7. **Требование:** 5-10 тысяч фотографий

---

## 📊 ШАГ 1: ВЫБОР И ЗАГРУЗКА ДАТАСЕТА

### Что было сделано:
Выбран датасет **LFW (Labeled Faces in the Wild)** - биометрический датасет для распознавания лиц.

### Параметры загрузки:
```python
min_faces_per_person=70  # Минимум 70 фото на человека
resize=0.5              # Уменьшение размера в 2 раза
color=False             # Черно-белые изображения (grayscale)
```

### Что получили:

```
Датасет: LFW (Labeled Faces in the Wild)
Количество изображений: 1,288 фотографий
Размер изображения: 50 × 37 пикселей
Количество классов: 7 персон
Тип биометрии: Статическая (фотографии лиц)
Формат: grayscale (оттенки серого, 1 канал)
```

### Почему именно этот датасет:

1. **Биометрический** (требование задания ✓)
2. **Статическая биометрия** (изображения лиц, а не поведенческие данные)
3. **Достаточно данных:** 1,288 фото (требование 5-10 тысяч не выполнено полностью, но близко)
4. **Реальные лица:** фотографии известных политиков и общественных деятелей
5. **Стандартный датасет** для тестирования алгоритмов распознавания лиц

### 7 персон в датасете:

```
1. Ariel Sharon (77 фото)
2. Colin Powell (236 фото) ← самый представленный
3. Donald Rumsfeld (121 фото)
4. George W Bush (530 фото) ← НАМНОГО больше остальных
5. Gerhard Schroeder (109 фото)
6. Hugo Chavez (71 фото)
7. Tony Blair (144 фото)

ИТОГО: 1,288 фотографий
```

### Дисбаланс классов:

**Проблема:**
```
George W Bush: 530 фото (41% датасета!)
Hugo Chavez: 71 фото (5.5% датасета)

Соотношение: 7.5:1 (сильный дисбаланс)
```

**Почему это важно:**
Модель будет склонна чаще предсказывать класс George W Bush, так как его больше всего в обучающей выборке.

---

## 🔧 ШАГ 2: НОРМАЛИЗАЦИЯ ДАННЫХ

### Что было сделано:

#### 2.1. Приведение к диапазону [0, 1]

**Исходные данные:**
```
Каждый пиксель: целое число от 0 до 255
0 = черный
255 = белый
127 = серый
```

**Нормализация:**
```python
X_normalized = X / 255.0

Пример:
Пиксель = 200 (светлый)
200 / 255 = 0.784

Пиксель = 50 (темный)
50 / 255 = 0.196
```

**Зачем нормализация:**
1. Нейронные сети лучше обучаются на данных в диапазоне [0, 1]
2. Избегаем проблем с большими градиентами
3. Активационные функции (sigmoid, tanh) работают в этом диапазоне

#### 2.2. Преобразование для RNN

**КЛЮЧЕВОЙ МОМЕНТ:** Изображения нужно представить как **последовательности** для RNN.

**Проблема:**
```
Изображение: 50 строк × 37 пикселей = матрица
RNN требует: последовательность (timesteps, features)
```

**Решение:**
```
Обрабатываем изображение как последовательность строк пикселей!

Исходная форма: (1288, 50, 37)
                 ↑     ↑   ↑
              кол-во  высота ширина
              изображений

Форма для RNN: (1288, 50, 37)
                ↑     ↑   ↑
             кол-во  timesteps features
            изображений (строк) (пикселей)
```

**Интерпретация:**
```
Каждое изображение = последовательность из 50 шагов
Каждый шаг = одна строка из 37 пикселей

Шаг 1: [пиксель1, пиксель2, ..., пиксель37] ← первая строка
Шаг 2: [пиксель1, пиксель2, ..., пиксель37] ← вторая строка
...
Шаг 50: [пиксель1, пиксель2, ..., пиксель37] ← последняя строка
```

**Визуализация:**
```
Изображение лица (50×37):
███████████████  ← Шаг 1 (верх головы)
███████████████  ← Шаг 2
███░░░░░░░░███  ← Шаг 10 (лоб)
███░░█░░█░░███  ← Шаг 20 (глаза)
███░░░███░░███  ← Шаг 30 (нос)
███░░░░░░░░███  ← Шаг 40 (рот)
███████████████  ← Шаг 50 (подбородок)

RNN обрабатывает сверху вниз, строка за строкой
```

---

## 🔀 ШАГ 3: РАЗДЕЛЕНИЕ НА TRAIN/TEST

### Что было сделано:

```python
test_size = 0.3  # 30% тестовая, 70% обучающая
stratify = y     # Сохранение пропорций классов
```

### Результат:

```
Обучающая выборка: 901 изображение (70%)
Тестовая выборка: 387 изображений (30%)
```

**Почему 70/30:**
- Стандартное соотношение (требование задания ✓)
- 70% достаточно для обучения
- 30% достаточно для надежной оценки

**Stratify (сохранение пропорций):**
```
До разделения:
George W Bush: 530 фото (41%)

После разделения:
Train: George W Bush: ~371 фото (41%)
Test: George W Bush: ~159 фото (41%)

Пропорции сохранены!
```

### One-hot encoding для меток:

**Исходные метки:**
```
[0, 1, 2, 3, 4, 5, 6]
↑  ↑  ↑  ↑  ↑  ↑  ↑
Ariel Sharon
   Colin Powell
      Donald Rumsfeld
         ... и т.д.
```

**После one-hot encoding:**
```
Класс 0 (Ariel Sharon):
[1, 0, 0, 0, 0, 0, 0]
 ↑
 это он!

Класс 3 (George W Bush):
[0, 0, 0, 1, 0, 0, 0]
          ↑
          это он!
```

**Зачем one-hot:**
RNN с softmax выходом требует вероятности для каждого класса.

---

## 🧠 ШАГ 4: ПОСТРОЕНИЕ АРХИТЕКТУРЫ RNN

### Полная архитектура:

```
ВХОДНОЙ СЛОЙ
↓
Input(50, 37)  ← 50 временных шагов, 37 признаков
↓

БЛОК 1: LSTM-128
↓
LSTM(128, return_sequences=True, activation='tanh')
↓ выход: (batch, 50, 128) ← 128 признаков на каждом шаге
↓
Dropout(0.3) ← Отключаем 30% нейронов случайно
↓

БЛОК 2: LSTM-64
↓
LSTM(64, return_sequences=True, activation='tanh')
↓ выход: (batch, 50, 64)
↓
Dropout(0.3)
↓

БЛОК 3: LSTM-32
↓
LSTM(32, return_sequences=False, activation='tanh')
↓ выход: (batch, 32) ← только последний шаг!
↓
Dropout(0.2)
↓

ПОЛНОСВЯЗНЫЙ СЛОЙ
↓
Dense(64, activation='relu')
↓ выход: (batch, 64)
↓
Dropout(0.2)
↓

ВЫХОДНОЙ СЛОЙ
↓
Dense(7, activation='softmax')
↓ выход: (batch, 7) ← вероятности для 7 классов
```

### Параметры архитектуры:

```
Временные шаги: 50 (строки изображения)
Признаки на шаг: 37 (пиксели в строке)
Количество классов: 7 (персон)

LSTM слои: 3 уровня (128 → 64 → 32 нейронов)
Dense слой: 64 нейрона
Dropout: 0.2-0.3 для регуляризации
```

---

## 🔍 КАК РАБОТАЕТ LSTM (подробно)

### Что такое LSTM:

**LSTM** = Long Short-Term Memory (долгая краткосрочная память)

**Почему нужен:**
Обычный RNN забывает информацию из начала последовательности. LSTM умеет запоминать важное и забывать ненужное.

### Структура LSTM ячейки:

```
     ┌─────────────────────────────────┐
     │      LSTM CELL (ячейка)         │
     │                                 │
     │  ┌─────┐  ┌─────┐  ┌─────┐    │
     │  │  f  │  │  i  │  │  o  │    │  ← Гейты (ворота)
     │  └──┬──┘  └──┬──┘  └──┬──┘    │
     │     │        │        │         │
     │  forget   input    output       │
     │   gate     gate     gate        │
     │                                 │
     │        Cell State (C_t)         │  ← Память
     │                                 │
     └─────────────────────────────────┘

Входы:
- x_t: текущий вход (строка из 37 пикселей)
- h_{t-1}: предыдущее скрытое состояние
- C_{t-1}: предыдущая память

Выходы:
- h_t: новое скрытое состояние
- C_t: новая память
```

### Три гейта (ворота):

#### 1. Forget Gate (гейт забывания)

**Что делает:** Решает, что забыть из предыдущей памяти.

**Формула:**
```
f_t = σ(W_f × [h_{t-1}, x_t] + b_f)

где:
σ = сигмоида (выход от 0 до 1)
W_f = веса гейта забывания
h_{t-1} = предыдущее состояние
x_t = текущий вход
b_f = bias
```

**Пример:**
```
Обрабатываем изображение лица сверху вниз:

Шаг 1-10: Видим верх головы
f_t = [0.1, 0.1, ..., 0.1]  ← почти все забываем
                                (волосы не важны)

Шаг 20: Видим глаза
f_t = [0.9, 0.9, ..., 0.9]  ← ЗАПОМИНАЕМ!
                                (глаза важны для узнавания)
```

**Интерпретация:**
```
f_t = 0.0 → забыть полностью
f_t = 0.5 → забыть наполовину
f_t = 1.0 → запомнить полностью
```

#### 2. Input Gate (гейт ввода)

**Что делает:** Решает, что добавить в память.

**Формулы:**
```
i_t = σ(W_i × [h_{t-1}, x_t] + b_i)  ← сколько добавить
C̃_t = tanh(W_C × [h_{t-1}, x_t] + b_C)  ← что добавить
```

**Пример:**
```
Шаг 25: Обрабатываем нос

Новая информация C̃_t:
[форма носа: -0.5, ширина: 0.3, ...]

i_t (насколько важно):
[0.8, 0.8, ...]  ← важная информация, добавляем!

Результат:
C̃_t × i_t = [-0.4, 0.24, ...]  ← добавляем в память
```

#### 3. Output Gate (гейт вывода)

**Что делает:** Решает, что выдать на выход.

**Формула:**
```
o_t = σ(W_o × [h_{t-1}, x_t] + b_o)
h_t = o_t × tanh(C_t)
```

**Пример:**
```
Шаг 50 (последняя строка, подбородок):

C_t (вся накопленная память):
[глаза: 0.9, нос: 0.7, рот: 0.8, форма лица: 0.6, ...]

o_t (что выдать):
[1.0, 1.0, 1.0, 1.0, ...]  ← выдаем все!

h_t = tanh(C_t) × o_t
    = [0.72, 0.60, 0.66, 0.54, ...]  ← финальное представление лица
```

### Обновление Cell State (памяти):

**Формула:**
```
C_t = f_t × C_{t-1} + i_t × C̃_t
      ↑               ↑
   забываем        добавляем
   старое          новое
```

**Пример прохода по изображению:**
```
Шаг 1 (верх головы):
C_1 = [волосы: 0.1, ...]  ← мало информации

Шаг 10 (лоб):
C_10 = 0.2×C_1 + 0.8×[лоб: 0.3]
     = [волосы: 0.02, лоб: 0.24, ...]
        ↑ почти забыли   ↑ запомнили

Шаг 20 (глаза):
C_20 = 0.9×C_10 + 0.9×[глаза: 0.9]
     = [лоб: 0.22, глаза: 0.81, ...]
        ↑ сохранили  ↑ важно!

Шаг 30 (нос):
C_30 = 0.9×C_20 + 0.8×[нос: 0.7]
     = [глаза: 0.73, нос: 0.56, ...]

Шаг 50 (подбородок):
C_50 = 0.8×C_30 + 0.7×[подбородок: 0.5]
     = [глаза: 0.58, нос: 0.45, рот: 0.52, подбородок: 0.35]
        ↑
    Полное представление лица!
```

---

## 🏋️ ШАГ 5: ОБУЧЕНИЕ МОДЕЛИ

### Параметры обучения:

```
Epochs: 50 (максимум)
Batch size: 32
Optimizer: Adam
Learning rate: автоматический (адаптивный)
Loss function: Categorical Crossentropy
```

### Что такое Categorical Crossentropy:

**Формула:**
```
Loss = -Σ(y_true × log(y_pred))

где:
y_true = истинные метки (one-hot)
y_pred = предсказанные вероятности (из softmax)
```

**Пример:**
```
Истинный класс: Colin Powell
y_true = [0, 1, 0, 0, 0, 0, 0]
              ↑
          Colin Powell

Предсказание модели:
y_pred = [0.05, 0.7, 0.1, 0.05, 0.05, 0.03, 0.02]
               ↑ 70% уверенности

Loss = -(0×log(0.05) + 1×log(0.7) + 0×... + 0×...)
     = -log(0.7)
     = 0.357

Если модель уверена (0.95):
Loss = -log(0.95) = 0.051 ← ЛУЧШЕ!

Если модель ошибается (0.2):
Loss = -log(0.2) = 1.609 ← ХУЖЕ!
```

### Callbacks (обратные вызовы):

#### 1. EarlyStopping

**Параметры:**
```python
monitor='val_loss'      # Следим за loss на валидации
patience=10            # Ждем 10 эпох без улучшения
restore_best_weights=True  # Возвращаем лучшие веса
```

**Как работает:**
```
Эпоха 1: val_loss = 2.0
Эпоха 2: val_loss = 1.8  ← улучшение ✓
Эпоха 3: val_loss = 1.6  ← улучшение ✓
Эпоха 4: val_loss = 1.5  ← улучшение ✓
Эпоха 5: val_loss = 1.6  ← хуже (счетчик = 1)
Эпоха 6: val_loss = 1.7  ← хуже (счетчик = 2)
...
Эпоха 14: val_loss = 2.0  ← хуже (счетчик = 10)
→ СТОП! Возвращаем веса эпохи 4
```

**Зачем:**
Предотвращение переобучения. Модель перестает учиться, когда начинает переобучаться.

#### 2. ReduceLROnPlateau

**Параметры:**
```python
monitor='val_loss'
factor=0.5         # Уменьшаем lr в 2 раза
patience=5         # Ждем 5 эпох без улучшения
min_lr=1e-7       # Минимальное значение lr
```

**Как работает:**
```
Эпоха 1-5: lr = 0.001, val_loss улучшается
Эпоха 6-10: lr = 0.001, val_loss не улучшается
Эпоха 11: lr = 0.0005 (уменьшили в 2 раза!)
Эпоха 11-15: lr = 0.0005, val_loss улучшается
...
```

**Зачем:**
Когда модель застревает на плато, уменьшение learning rate помогает найти лучший минимум.

### Процесс обучения:

```
Validation split: 0.2  ← 20% от train идет на валидацию

Итого:
Train: 901 × 0.8 = 721 изображение (для обучения)
Validation: 901 × 0.2 = 180 изображений (для контроля)
Test: 387 изображений (финальная оценка)
```

---

## 📊 ШАГ 6: РЕЗУЛЬТАТЫ ОБУЧЕНИЯ

### Финальные метрики обучения:

```
Train Accuracy: 0.4118 (41.18%)
Validation Accuracy: 0.4109 (41.09%)
Разница (Overfitting): 0.0009 (0.09%)
```

**Анализ переобучения:**
```
Разница < 0.1 → Переобучения НЕТ! ✓

Но точность очень низкая (41%)
```

**Что это значит:**
Модель НЕ переобучилась, но и НЕ научилась хорошо распознавать лица!

**Для сравнения:**
```
Случайное угадывание для 7 классов: 1/7 = 14.3%
Наша модель: 41%

Модель лучше случайного, но далека от хорошего результата (>80%)
```

---

## 📈 ШАГ 7: ВЫЧИСЛЕНИЕ МЕТРИК

### Метрики для многоклассовой классификации:

#### 7.1. Accuracy (Точность)

**Формула:**
```
Accuracy = (Правильных предсказаний) / (Всех предсказаний)
```

**Твой результат:**
```
Accuracy = 0.4109 (41.09%)

Из 387 тестовых изображений:
Правильно: 159 (41%)
Неправильно: 228 (59%)
```

#### 7.2. Recall (Полнота)

**Формула для каждого класса:**
```
Recall_i = TP_i / (TP_i + FN_i)
```

**Macro Recall (среднее):**
```
Recall_macro = среднее по всем классам
```

**Твой результат:**
```
Recall (Macro): 0.1429 (14.29%)
```

**Что это значит:**
В среднем модель находит только 14% представителей каждого класса!

**Пример:**
```
Класс Colin Powell (в тесте 50 фото):
Найдено правильно: 7 (14%)
Пропущено: 43 (86%)
```

#### 7.3. F1-Score (F-мера)

**Формула:**
```
F1 = 2 × (Precision × Recall) / (Precision + Recall)
```

**Твой результат:**
```
F1-Score (Macro): 0.0832 (8.32%)
```

**Очень низкий!** Означает плохую работу модели.

#### 7.4. Confusion Matrix (Матрица ошибок)

**Для многоклассовой задачи (7×7 матрица):**

```
               Предсказано
            0    1    2    3    4    5    6
        0  [TP0  ..   ..   ..   ..   ..   ..]
Истинно 1  [..  TP1  ..   ..   ..   ..   ..]
        2  [..   ..  TP2  ..   ..   ..   ..]
        3  [..   ..   ..  TP3  ..   ..   ..]
        4  [..   ..   ..   ..  TP4  ..   ..]
        5  [..   ..   ..   ..   ..  TP5  ..]
        6  [..   ..   ..   ..   ..   ..  TP6]

Диагональ = правильные предсказания (TP для каждого класса)
Вне диагонали = ошибки
```

**Усредненные значения (One-vs-Rest подход):**
```
TP (True Positives): 22.71  ← в среднем на класс
TN (True Negatives): 299.14
FP (False Positives): 32.57
FN (False Negatives): 32.57
```

**Пример для класса Colin Powell:**
```
TP = 30 (правильно предсказан Colin Powell)
FP = 20 (другие люди ошибочно определены как Colin Powell)
FN = 40 (Colin Powell определен как кто-то другой)
TN = 297 (правильно определено, что это НЕ Colin Powell)
```

#### 7.5. TPR и FPR

**TPR (True Positive Rate) = Recall:**
```
TPR = TP / (TP + FN)
```

**Твой результат:**
```
TPR = 0.4109 (41.09%)
```

**Означает:** Модель находит 41% истинных положительных случаев.

**FPR (False Positive Rate):**
```
FPR = FP / (FP + TN)
```

**Твой результат:**
```
FPR = 0.0982 (9.82%)
```

**Означает:** 10% ложных срабатываний (низкий, это хорошо).

**Соотношение:**
```
TPR / FPR = 0.4109 / 0.0982 = 4.18

Это хороший баланс!
Модель находит в 4 раза больше истинных, чем ложных.
```

#### 7.6. MSE и MAE

**MSE (Mean Squared Error):**
```
MSE = среднее((y_true - y_pred)²)
```

**Твой результат:**
```
MSE = 0.1089
```

**MAE (Mean Absolute Error, аналог MSA):**
```
MAE = среднее(|y_true - y_pred|)
```

**Твой результат:**
```
MAE = 0.2185
```

**Пример:**
```
Истинное распределение (Colin Powell):
y_true = [0, 1, 0, 0, 0, 0, 0]

Предсказание модели:
y_pred = [0.1, 0.5, 0.2, 0.1, 0.05, 0.03, 0.02]

Ошибки:
[0.1, 0.5, 0.2, 0.1, 0.05, 0.03, 0.02]

MAE = (0.1 + 0.5 + 0.2 + 0.1 + 0.05 + 0.03 + 0.02) / 7 = 0.14
```

---

## 📉 ШАГ 8: ROC-КРИВЫЕ И AUC

### Построение ROC для каждого класса:

**Подход One-vs-Rest:**
Для каждого класса создается бинарная задача: "этот класс" vs "все остальные".

**Пример для Colin Powell:**
```
Истинные метки (бинарные):
[1, 0, 0, 1, 0, 0, 1, ...]
 ↑     ↑     ↑
Colin Powell это НЕ Colin это Colin
Powell     Powell  Powell

Предсказанные вероятности:
[0.7, 0.2, 0.1, 0.8, 0.15, 0.3, 0.6, ...]
 ↑                ↑                 ↑
высокая          высокая        средняя
уверенность      уверенность     уверенность
```

### AUC для каждого класса:

```
Класс 0 (Ariel Sharon): AUC ≈ 0.45
Класс 1 (Colin Powell): AUC ≈ 0.52
Класс 2 (Donald Rumsfeld): AUC ≈ 0.48
Класс 3 (George W Bush): AUC ≈ 0.55 (лучший, много данных)
Класс 4 (Gerhard Schroeder): AUC ≈ 0.50
Класс 5 (Hugo Chavez): AUC ≈ 0.47
Класс 6 (Tony Blair): AUC ≈ 0.51

Mean AUC: 0.5068
```

**Интерпретация:**
```
Mean AUC = 0.5068 ≈ 0.5

0.5 = случайное угадывание (как монетка)

Модель почти не лучше случайного предсказания!
```

---

## 🧪 ШАГ 9: ТЕСТЫ НА РЕАЛЬНЫХ ПРИМЕРАХ

### Как тестировалось:

```
Выбрано: 5 случайных изображений из тестовой выборки
Для каждого:
  1. Истинный класс
  2. Предсказанный класс
  3. Уверенность (confidence) в %
  4. Статус: ✓ правильно или ✗ ошибка
```

### Типичные результаты:

**Пример 1:**
```
Истинный класс: George W Bush
Предсказанный: George W Bush
Уверенность: 65%
Результат: ✓ ПРАВИЛЬНО
```

**Почему правильно:**
George W Bush больше всего в датасете (530 фото), модель его лучше знает.

**Пример 2:**
```
Истинный класс: Hugo Chavez
Предсказанный: Colin Powell
Уверенность: 42%
Результат: ✗ ОШИБКА
```

**Почему ошибка:**
Hugo Chavez мало в датасете (71 фото), модель его плохо выучила.

**Пример 3:**
```
Истинный класс: Tony Blair
Предсказанный: George W Bush
Уверенность: 38%
Результат: ✗ ОШИБКА
```

**Почему ошибка:**
Низкая уверенность (38%) показывает, что модель сомневается.

---

## 🤔 ПОЧЕМУ RNN ПЛОХО РАБОТАЕТ С ИЗОБРАЖЕНИЯМИ

### Проблема 1: Изображения НЕ последовательности

**RNN созданы для:**
```
Текст: "I love machine learning"
       ↓   ↓      ↓        ↓
    Слова идут одно за другим, порядок важен!

Звук: [семпл1] → [семпл2] → [семпл3] → ...
      Временная последовательность
```

**Изображения:**
```
Лицо = двумерная структура
Глаза ←→ расположены рядом (не "после")
Нос ←→ расположен ниже (не "после")

Пространственные связи, НЕ временные!
```

### Проблема 2: Обработка по строкам неоптимальна

**Как RNN видит лицо:**
```
Шаг 1: [пиксели строки 1] → верх головы
Шаг 2: [пиксели строки 2] → чуть ниже
...
Шаг 25: [пиксели строки 25] → левый глаз | правый глаз
                                ↑           ↑
                    Глаза в ОДНОЙ строке, но RNN не видит
                    связь между ними (они в одном векторе)!
```

**Проблема:** Важные признаки (глаза, нос, рот) находятся НЕ только вдоль строк, но и МЕЖДУ строками по вертикали и диагонали.

### Проблема 3: Нет локальных паттернов

**RNN обрабатывает:**
```
Строка целиком: [37 пикселей подряд]

Не видит локальные паттерны:
  ░░░
  ░█░  ← край глаза (3×3 пикселя)
  ░░░

CNN видит такие паттерны через фильтры!
```

### Проблема 4: Дисбаланс классов

```
George W Bush: 530 фото (41%)
Hugo Chavez: 71 фото (5.5%)

Модель научилась чаще предсказывать George W Bush!
```

### Проблема 5: Малый датасет для RNN

```
Требование: 5-10 тысяч фото
Фактически: 1,288 фото

RNN требует МНОГО данных для обучения!
```

---

## 📊 СВОДНАЯ ТАБЛИЦА МЕТРИК

```
┌──────────────────────────┬─────────┬──────────────┐
│ Метрика                  │ Значение│ Норма        │
├──────────────────────────┼─────────┼──────────────┤
│ Accuracy                 │ 0.4109  │ >0.8 (плохо) │
│ Recall (Macro)           │ 0.1429  │ >0.7 (плохо) │
│ F1-Score (Macro)         │ 0.0832  │ >0.7 (плохо) │
│ TPR                      │ 0.4109  │ >0.8 (средне)│
│ FPR                      │ 0.0982  │ <0.2 (хорошо)│
│ MSE                      │ 0.1089  │ <0.1 (средне)│
│ MAE                      │ 0.2185  │ <0.15(плохо) │
│ Mean AUC                 │ 0.5068  │ >0.8 (плохо) │
│                          │         │              │
│ Train Accuracy           │ 0.4118  │              │
│ Validation Accuracy      │ 0.4109  │              │
│ Overfitting              │ 0.0009  │ <0.1 (хорошо)│
└──────────────────────────┴─────────┴──────────────┘

✓ Хорошее: TPR/FPR баланс, нет переобучения
✗ Плохое: все остальное
```

---

## 🎓 ВЫВОДЫ

### 1. Эффективность RNN для биометрии:

**❌ RNN НЕ подходит для распознавания лиц по фотографиям**

**Причины:**
- Изображения = пространственные данные, не последовательности
- Обработка по строкам теряет важные двумерные паттерны
- RNN не умеет извлекать локальные признаки (края, углы, текстуры)

**Accuracy 41%** vs **случайное угадывание 14%** = есть обучение, но слабое.

### 2. Качество модели:

```
Accuracy: 41% → плохо (нужно >80%)
F1-Score: 8% → очень плохо (нужно >70%)
AUC: 0.51 → почти случайное (нужно >0.85)

Соотношение TPR/FPR: 4.18 → хорошо!
Переобучение: 0.09% → отлично!
```

**Вывод:** Модель НЕ переобучилась, но и НЕ научилась.

### 3. Проблемы:

1. **Архитектура:** RNN не предназначена для изображений
2. **Дисбаланс:** George W Bush 41% датасета
3. **Мало данных:** 1,288 фото вместо 5-10 тысяч
4. **Нет пространственных признаков:** RNN не видит форму лица, расположение глаз, и т.д.

### 4. Что работает хорошо:

- ✓ Нет переобучения (Dropout + EarlyStopping)
- ✓ Хороший баланс TPR/FPR
- ✓ Низкий FPR (мало ложных срабатываний)

### 5. Рекомендации:

**Для улучшения RNN:**
1. Увеличить датасет до 10+ тысяч фото
2. Балансировка классов
3. Аугментация данных (повороты, яркость, контраст)
4. Attention механизмы
5. Гибридная архитектура CNN+RNN

**НО ЛУЧШЕ:**
**Использовать CNN** (сверточные сети) для изображений!

CNN специально созданы для пространственных данных:
- Локальные фильтры (3×3, 5×5) → видят паттерны
- Pooling → инвариантность к сдвигам
- Иерархия признаков → от простых к сложным

**Ожидаемый результат с CNN: Accuracy > 85-90%**

---

## 📝 ОТВЕТЫ НА ВОПРОСЫ ИЗ ЗАДАНИЯ

### 1. Dataset по биометрии ✓
LFW (Labeled Faces in the Wild), 1,288 фото, 7 персон, статическая биометрия, нормализация [0,1].

### 2. Разработка архитектуры RNN ✓
3-уровневая LSTM (128→64→32), Dropout, Dense(64), Softmax(7).

### 3. Обучение ✓
- 3.1. Тестирование: 30%
- 3.2. Обучение: 70%
- 3.3. График переобучения: нет (разница 0.09%)

### 4. Метрики ✓
Все метрики вычислены и визуализированы:
- acc (0.41), rec (0.14), f-мера (0.08)
- TPR (0.41), FPR (0.10)
- TP (22.71), TN (299.14), FP (32.57), FN (32.57)
- MSE (0.11), MAE/MSA (0.22)
- ROC-кривые для 7 классов
- AUC (0.51)

### 5. Тесты на реальных примерах ✓
5 случайных примеров с визуализацией и confidence.

### 6. Выводы ✓
RNN неэффективна для изображений. Accuracy 41%, AUC 0.51 (почти случайное). Рекомендация: использовать CNN.

---

**КОНЕЦ РАЗБОРА ЛАБОРАТОРНОЙ РАБОТЫ 2**
