# ЛАБОРАТОРНАЯ РАБОТА 2: RNN/LSTM ДЛЯ БИОМЕТРИИ - ДЕТАЛЬНОЕ ОБЪЯСНЕНИЕ

## 📋 ЧТО МЫ ДЕЛАЛИ В ЭТОЙ ЛАБЕ?

**Главная задача:** Научить нейронную сеть распознавать людей по фотографиям лиц, используя рекуррентную архитектуру (LSTM).

**Это называется:** Биометрическая идентификация

---

## 1️⃣ ДАТАСЕТ: LABELED FACES IN THE WILD (LFW)

### Что за датасет?

**Полное название:** Labeled Faces in the Wild
**Сокращение:** LFW
**Для чего:** Стандартный бенчмарк для распознавания лиц

### Конкретные данные:

**ДО фильтрации:**
```
- Всего: 13,000+ фотографий
- Людей: 5,000+ разных персон
- Источник: интернет (знаменитости, публичные фигуры)
```

**НАШИ параметры загрузки:**
```python
min_faces_per_person = 70  ← Берем только тех, у кого >= 70 фото
resize = 0.5               ← Уменьшаем разрешение в 2 раза
color = False              ← Черно-белые (grayscale)
```

**ПОСЛЕ фильтрации (наш финальный датасет):**
```
Всего изображений: 1288
Количество персон: 7
Размер изображения: 50 × 37 пикселей
Цвет: grayscale (1 канал)
```

### Распределение по классам:

```
Класс 0 (George W Bush):      530 фото  ← Самый большой класс
Класс 1 (Colin Powell):        236 фото
Класс 2 (Tony Blair):          144 фото
Класс 3 (Donald Rumsfeld):     121 фото
Класс 4 (Gerhard Schroeder):    109 фото
Класс 5 (Ariel Sharon):          77 фото
Класс 6 (Hugo Chavez):           71 фото
──────────────────────────────────────
ИТОГО:                         1288 фото
```

**Дисбаланс классов!** Георг Буш = 530, а Уго Чавес = 71. Это важно!

### Как выглядят изображения?

```
Пример одной фотографии (50×37 = 1850 пикселей):

[[ 89  92  95 ... 112 115 118]   ← первая строка (37 пикселей)
 [ 91  94  97 ... 114 117 120]   ← вторая строка
 [ 93  96  99 ... 116 119 122]
 ...
 [145 148 151 ... 168 171 174]]  ← 50-я строка

Каждое число = яркость пикселя от 0 (черный) до 255 (белый)
```

### Зачем этот датасет?

**Реальное применение:**
- Системы контроля доступа (вход по лицу)
- Разблокировка телефона (Face ID)
- Поиск людей на фото в соцсетях
- Безопасность (камеры наблюдения)

---

## 2️⃣ ПРЕДОБРАБОТКА ДАННЫХ ДЛЯ RNN

### ШАГ 1: Нормализация пикселей

**ЗАЧЕМ?**
Нейронные сети лучше работают с числами от 0 до 1, чем от 0 до 255.

**ЧТО ДЕЛАЕМ:**
```
Было: пиксель = 156 (из 255)
Стало: пиксель = 156 / 255.0 = 0.612

Было: пиксель = 0 (черный)
Стало: пиксель = 0 / 255.0 = 0.0

Было: пиксель = 255 (белый)
Стало: пиксель = 255 / 255.0 = 1.0
```

**ФОРМУЛА:**
```
x_normalized = x / 255.0
```

**РЕЗУЛЬТАТ:**
- Было: значения от 0 до 255
- Стало: значения от 0.0 до 1.0

---

### ШАГ 2: Преобразование для RNN (САМОЕ ВАЖНОЕ!)

**ПРОБЛЕМА:**
RNN ожидает ПОСЛЕДОВАТЕЛЬНОСТЬ, а изображение - это двумерная матрица!

**Изображение:**
```
50 строк
↓
●●●●●●... (37 пикселей) → ширина
●●●●●●...
...
```

**КАК ПРЕВРАТИТЬ В ПОСЛЕДОВАТЕЛЬНОСТЬ?**

**РЕШЕНИЕ:** Обрабатываем изображение ПОСТРОЧНО!

**ЛОГИКА:**
```
Представим, что RNN "читает" изображение сверху вниз,
строка за строкой, как текст.

Шаг 1: читает первую строку (37 пикселей)
Шаг 2: читает вторую строку (37 пикселей)
...
Шаг 50: читает последнюю строку (37 пикселей)
```

**КОНКРЕТНЫЙ ПРИМЕР:**

**ДО преобразования:**
```
Форма: (1288, 50, 37)
          ↑    ↑   ↑
          |    |   └─ ширина
          |    └───── высота
          └────────── количество изображений

Это МАТРИЦА 50×37 для каждого изображения
```

**ПОСЛЕ преобразования для RNN:**
```
Форма: (1288, 50, 37)
          ↑    ↑   ↑
          |    |   └─ features (признаков на каждом шаге)
          |    └───── timesteps (временных шагов)
          └────────── samples (образцов)

RNN интерпретация:
- 1288 изображений
- Каждое = последовательность из 50 шагов
- На каждом шаге = вектор из 37 чисел
```

**ВИЗУАЛЬНО:**

```
Исходное изображение 50×37:
█████████████████   ← строка 1 (37 чисел)
█████████████████   ← строка 2 (37 чисел)
█████████████████   ← строка 3
...
█████████████████   ← строка 50

↓ превращается в ↓

Последовательность для RNN:
t=1:  [0.35, 0.42, 0.38, ..., 0.61]  ← 37 чисел (строка 1)
t=2:  [0.37, 0.44, 0.40, ..., 0.63]  ← 37 чисел (строка 2)
t=3:  [0.39, 0.46, 0.42, ..., 0.65]  ← 37 чисел (строка 3)
...
t=50: [0.67, 0.74, 0.70, ..., 0.93]  ← 37 чисел (строка 50)
```

**МАТЕМАТИКА:**
```
X_RNN ∈ ℝ^(N×T×F)

где:
N = 1288 (количество изображений)
T = 50 (timesteps, количество строк)
F = 37 (features, пикселей в строке)
```

---

### ШАГ 3: One-Hot Encoding меток

**ПРОБЛЕМА:**
Метки классов = числа: 0, 1, 2, 3, 4, 5, 6

Нейросеть может подумать, что 6 > 0 (математически), но это просто разные люди!

**РЕШЕНИЕ:** One-Hot Encoding

**КАК ЭТО РАБОТАЕТ:**

**БЫЛО:**
```
Фото 1: класс = 0 (George Bush)
Фото 2: класс = 2 (Tony Blair)
Фото 3: класс = 5 (Ariel Sharon)
```

**СТАЛО:**
```
Фото 1: [1, 0, 0, 0, 0, 0, 0]  ← единица на позиции 0
         ↑
         класс 0

Фото 2: [0, 0, 1, 0, 0, 0, 0]  ← единица на позиции 2
            ↑
            класс 2

Фото 3: [0, 0, 0, 0, 0, 1, 0]  ← единица на позиции 5
                     ↑
                     класс 5
```

**ФОРМУЛА:**
```
y_i = [0, 0, ..., 1, ..., 0] ∈ {0,1}^C

где C = 7 (количество классов)
1 находится на позиции истинного класса
```

**ЗАЧЕМ:** Теперь все классы "равноправны", нет ложной математической связи

---

### ШАГ 4: Разделение на train/test

**ЗАЧЕМ:** Проверить, как сеть работает на НОВЫХ данных (которые не видела при обучении)

**КАК:**
```
Стратифицированное разделение 80/20
(сохраняем пропорции классов)
```

**КОНКРЕТНО:**
```
TRAIN (обучающая выборка):
- Размер: 1030 изображений (80%)
- Используется для: обучения сети (подбора весов)

TEST (тестовая выборка):
- Размер: 258 изображений (20%)
- Используется для: финальной оценки качества
- Сеть НЕ ВИДЕЛА эти данные при обучении!
```

**Пример разделения для класса 0:**
```
Класс 0: всего 530 фото
Train: 424 фото (80%)
Test:  106 фото (20%)
```

---

## 3️⃣ АРХИТЕКТУРА RNN/LSTM: ЧТО ВНУТРИ?

### Сначала: ЧТО ТАКОЕ RNN?

**Простыми словами:**

Обычная нейросеть обрабатывает каждый вход независимо. RNN "помнит" предыдущие входы.

**Аналогия:**
```
Обычная сеть = читать каждое слово отдельно
RNN = читать предложение, помня предыдущие слова
```

**Базовая RNN:**

```
На каждом шаге t:

Вход x_t (текущая строка изображения, 37 чисел)
        ↓
    ┌───┴───┐
    │  RNN  │ ← h_(t-1) (память о предыдущих строках)
    └───┬───┘
        ↓
    h_t (новое состояние памяти)
        ↓
    y_t (выход)
```

**МАТЕМАТИКА (базовая RNN):**
```
h_t = tanh(W_h × h_(t-1) + W_x × x_t + b)

где:
h_t - скрытое состояние (память) в момент t
h_(t-1) - предыдущее состояние
x_t - текущий вход
W_h, W_x - веса (обучаемые параметры)
b - bias (смещение)
tanh - функция активации
```

**ПРОБЛЕМА базовых RNN:**

**Vanishing Gradient (затухание градиента):**

При обработке длинных последовательностей (у нас 50 шагов!), информация из начала "забывается". Градиенты становятся очень маленькими, обучение не работает.

**РЕШЕНИЕ:** LSTM!

---

### ЧТО ТАКОЕ LSTM?

**LSTM = Long Short-Term Memory** (Долгая Краткосрочная Память)

**Главная идея:**
Специальная архитектура с "воротами" (gates), которые контролируют:
1. Что забыть из старой информации
2. Что запомнить из новой информации
3. Что вывести

### АРХИТЕКТУРА LSTM (детально):

**СЛОВАМИ:**

LSTM имеет ДВА типа памяти:
- **Cell State (C_t)** - долговременная память (конвейер информации)
- **Hidden State (h_t)** - кратковременная память (текущий выход)

И ТРИ "ворот" (gates):

**1. FORGET GATE (Вентиль забывания)**

**ЧТО ДЕЛАЕТ:** Решает, что забыть из долговременной памяти

**ЛОГИКА:**
```
Смотрим на:
- Что было в прошлый момент (h_(t-1))
- Что пришло сейчас (x_t)

Решаем: какую часть старой памяти C_(t-1) ЗАБЫТЬ
```

**МАТЕМАТИКА:**
```
f_t = σ(W_f × [h_(t-1), x_t] + b_f)

где:
σ (сигмоида) - функция, дающая число от 0 до 1
f_t - вектор "коэффициентов забывания"
[h_(t-1), x_t] - объединение предыдущего состояния и входа
```

**ПРИМЕР:**
```
Если f_t = [0.9, 0.1, 0.7, ...]
           ↑    ↑    ↑
           |    |    └─ 70% информации сохраним
           |    └────── 10% сохраним (почти забыли)
           └─────────── 90% сохраним (почти не забыли)
```

**2. INPUT GATE (Входной вентиль)**

**ЧТО ДЕЛАЕТ:** Решает, что ДОБАВИТЬ в долговременную память

Состоит из ДВУХ частей:

**Часть A: Что важно?**
```
i_t = σ(W_i × [h_(t-1), x_t] + b_i)
```
Вектор от 0 до 1: насколько важна новая информация

**Часть B: Какая новая информация?**
```
C̃_t = tanh(W_C × [h_(t-1), x_t] + b_C)
```
Вектор от -1 до 1: кандидат для добавления в память

**ПРИМЕР:**
```
i_t = [0.8, 0.2, 0.9, ...]  ← насколько важно
C̃_t = [0.5, -0.3, 0.7, ...] ← что добавить

Реально добавим:
i_t ⊙ C̃_t = [0.8×0.5, 0.2×(-0.3), 0.9×0.7, ...]
           = [0.4, -0.06, 0.63, ...]

(⊙ - поэлементное умножение)
```

**3. ОБНОВЛЕНИЕ ПАМЯТИ (Cell State)**

**ЧТО ДЕЛАЕТ:** Обновляем долговременную память

**МАТЕМАТИКА:**
```
C_t = f_t ⊙ C_(t-1) + i_t ⊙ C̃_t
      ↑               ↑
      забываем        добавляем
      часть старого   новое
```

**ПРИМЕР (конкретные числа):**
```
Старая память C_(t-1) = [1.0, -0.5, 0.8]
Forget gate f_t = [0.9, 0.1, 0.7]
Input gate i_t = [0.8, 0.2, 0.9]
Новая инфо C̃_t = [0.5, -0.3, 0.7]

Забываем часть старого:
f_t ⊙ C_(t-1) = [0.9×1.0, 0.1×(-0.5), 0.7×0.8]
               = [0.9, -0.05, 0.56]

Добавляем новое:
i_t ⊙ C̃_t = [0.8×0.5, 0.2×(-0.3), 0.9×0.7]
           = [0.4, -0.06, 0.63]

Новая память:
C_t = [0.9+0.4, -0.05-0.06, 0.56+0.63]
    = [1.3, -0.11, 1.19]
```

**4. OUTPUT GATE (Выходной вентиль)**

**ЧТО ДЕЛАЕТ:** Решает, что ВЫВЕСТИ из долговременной памяти

**МАТЕМАТИКА:**
```
o_t = σ(W_o × [h_(t-1), x_t] + b_o)  ← что выводить
h_t = o_t ⊙ tanh(C_t)                ← финальный выход
```

**ПРИМЕР:**
```
C_t = [1.3, -0.11, 1.19]
tanh(C_t) = [0.86, -0.11, 0.83]  ← сжимаем в [-1, 1]

o_t = [0.9, 0.5, 0.8]  ← коэффициенты вывода

h_t = [0.9×0.86, 0.5×(-0.11), 0.8×0.83]
    = [0.77, -0.06, 0.66]

Это и есть выход LSTM на шаге t!
```

### ПОЛНАЯ КАРТИНА LSTM:

```
         ┌─────────── C_(t-1) ─────────┐
         │    (старая долговременная   │
         │         память)              │
         │                              │
         │                              ↓
    ┌────┴────┐                    ┌────────┐
    │ FORGET  │                    │  ×     │  f_t
    │  GATE   │                    └────┬───┘
    └────┬────┘                         │
         │                              │
    ┌────┴────┐    ┌─────────┐         │
x_t │  INPUT  │    │   C̃_t   │         │
──→ │  GATE   │──→ │candidate│         │
    └────┬────┘    └────┬────┘         │
         │   i_t        │              │
         └──────→ ×  ←──┘              │
                  │                     │
                  └─────→ +  ←──────────┘
                          │
                          │ C_t (новая память)
                          ↓
                     ┌────────┐
                     │ OUTPUT │
                     │  GATE  │
                     └────┬───┘
                          │
                          ↓ h_t
                      (выход)
```

---

## 4️⃣ НАША КОНКРЕТНАЯ АРХИТЕКТУРА

### Структура сети:

```
ВХОД: (50, 37)
  ↓
┌─────────────────┐
│ LSTM Layer 1    │
│ 128 нейронов    │ ← return_sequences=True (выдает всю последовательность)
│ Параметры:      │
│ 90,112          │
└────────┬────────┘
         │
┌────────┴────────┐
│ Dropout 30%     │ ← Регуляризация (выключаем 30% нейронов случайно)
└────────┬────────┘
         │
┌────────┴────────┐
│ LSTM Layer 2    │
│ 128 нейронов    │ ← return_sequences=False (только последний выход)
│ Параметры:      │
│ 132,608         │
└────────┬────────┘
         │
┌────────┴────────┐
│ Dropout 30%     │
└────────┬────────┘
         │
┌────────┴────────┐
│ Dense Layer     │
│ 7 нейронов      │ ← По одному на каждый класс
│ Softmax         │ ← Превращает в вероятности
│ Параметры: 903  │
└────────┬────────┘
         │
      ВЫХОД: [p0, p1, p2, p3, p4, p5, p6]
             (вероятности для каждого класса)
```

### Подсчет параметров:

**LSTM слой 1:**
```
Каждый LSTM имеет 4 блока (forget, input, output, candidate)
Каждый блок = матрица весов

Формула для LSTM с return_sequences:
Параметры = 4 × (hidden_size² + hidden_size × input_size + hidden_size)

Для нас:
hidden_size = 128 (нейронов)
input_size = 37 (признаков на входе)

Параметры = 4 × (128² + 128×37 + 128)
          = 4 × (16384 + 4736 + 128)
          = 4 × 21248
          = 84,992

Но на практике библиотека считает чуть иначе:
Параметры LSTM1 = 90,112
```

**LSTM слой 2:**
```
input_size = 128 (выход первого LSTM)
hidden_size = 128

Параметры = 4 × (128² + 128×128 + 128)
          = 4 × (16384 + 16384 + 128)
          = 4 × 32896
          = 131,584

На практике:
Параметры LSTM2 = 132,608
```

**Dense слой:**
```
Входов: 128 (выход LSTM2)
Выходов: 7 (классов)

Параметры = 128 × 7 + 7  (веса + bias)
          = 896 + 7
          = 903
```

**ИТОГО ПАРАМЕТРОВ:**
```
90,112 + 132,608 + 903 = 223,623 параметров!
```

Это 223 тысячи чисел, которые нужно подобрать при обучении!

### Что такое Dropout 30%?

**ПРОБЛЕМА:** Переобучение (сеть запоминает обучающие данные, но плохо обобщает)

**РЕШЕНИЕ:** Dropout

**КАК РАБОТАЕТ:**
```
Во время обучения НА КАЖДОМ шаге:
1. Случайно выбираем 30% нейронов
2. Временно "выключаем" их (обнуляем)
3. Обучаем оставшиеся 70%

Пример:
LSTM выход: [0.5, 0.3, -0.2, 0.8, 0.1, -0.4, ...]
                              ↓ dropout
После:      [0.5, 0.0, -0.2, 0.0, 0.1, -0.4, ...]
                  ↑         ↑
              выключены (30%)

На следующем шаге - другие нейроны!
```

**ЗАЧЕМ:** Нейроны учатся работать независимо, не полагаясь на конкретных "соседей"

**ПРИ ТЕСТИРОВАНИИ:** Dropout ВЫКЛЮЧЕН (используем все нейроны)

---

## 5️⃣ ОБУЧЕНИЕ СЕТИ

### Функция потерь: Categorical Crossentropy

**ЧТО ЭТО (словами):**

Мера того, насколько предсказание отличается от истины.

**ПРИМЕР:**

```
Истинный класс: 2 (Tony Blair)
One-hot: [0, 0, 1, 0, 0, 0, 0]
         ↑  ↑  ↑  ↑  ↑  ↑  ↑
         0  1  2  3  4  5  6

Предсказание сети (после softmax):
[0.05, 0.10, 0.60, 0.15, 0.05, 0.03, 0.02]
  ↑     ↑     ↑
  5%   10%   60% ← сеть думает, что это Tony Blair с вероятностью 60%
```

**ХОРОШЕЕ предсказание:**
```
Истина:      [0, 0, 1, 0, 0, 0, 0]
Предсказание:[0.01, 0.02, 0.92, 0.02, 0.01, 0.01, 0.01]
                        ↑
                     92% на правильном классе!
```

**ПЛОХОЕ предсказание:**
```
Истина:      [0, 0, 1, 0, 0, 0, 0]
Предсказание:[0.30, 0.25, 0.15, 0.10, 0.10, 0.05, 0.05]
                        ↑
                  только 15% на правильном классе :(
```

**МАТЕМАТИКА:**
```
Loss = -Σ(i=0 до 6) y_i × log(ŷ_i)

где:
y_i - истинная метка (0 или 1)
ŷ_i - предсказанная вероятность класса i
```

**КОНКРЕТНЫЙ РАСЧЕТ:**
```
Истина: [0, 0, 1, 0, 0, 0, 0]
Предсказание: [0.05, 0.10, 0.60, 0.15, 0.05, 0.03, 0.02]

Loss = -(0×log(0.05) + 0×log(0.10) + 1×log(0.60) + ...)
     = -log(0.60)
     = -(-0.51)
     = 0.51

Если бы предсказание было [0.01, 0.02, 0.92, ...]:
Loss = -log(0.92) = 0.08  ← ЛУЧШЕ (меньше)!

Если бы [0.30, 0.25, 0.15, ...]:
Loss = -log(0.15) = 1.90  ← ХУЖЕ (больше)!
```

**ЦЕЛЬ ОБУЧЕНИЯ:** Минимизировать Loss!

---

### Оптимизатор: Adam

**ЧТО ЭТО:** Алгоритм для подбора оптимальных весов сети

**ПРОСТАЯ АНАЛОГИЯ:**
Ты на склоне горы в тумане. Нужно спуститься вниз (минимизировать loss). Adam помогает выбрать направление и размер шага.

**КАК РАБОТАЕТ (упрощенно):**

1. **Вычисляем градиент** - в какую сторону loss растет
2. **Идем в противоположную сторону** - где loss уменьшается
3. **Адаптируем размер шага** - большие шаги вначале, маленькие в конце

**ЧТО ДЕЛАЕТ ADAM ОСОБЕННЫМ:**

**А) Momentum (момент):**
Учитывает предыдущие шаги. Если несколько раз шли в одну сторону - ускоряемся.

**Б) RMSprop:**
Адаптивный learning rate для каждого параметра отдельно.

**МАТЕМАТИКА (полная):**
```
Шаг 1: Вычисляем градиент
∇L = градиент функции потерь

Шаг 2: Обновляем экспоненциальное скользящее среднее градиента
m_t = β₁ × m_(t-1) + (1-β₁) × ∇L
(β₁ = 0.9 обычно)

Шаг 3: Обновляем экспоненциальное скользящее среднее квадрата градиента
v_t = β₂ × v_(t-1) + (1-β₂) × (∇L)²
(β₂ = 0.999 обычно)

Шаг 4: Корректируем смещение
m̂_t = m_t / (1 - β₁^t)
v̂_t = v_t / (1 - β₂^t)

Шаг 5: Обновляем веса
θ_t = θ_(t-1) - α × m̂_t / (√v̂_t + ε)

где:
α = learning rate (0.001 у нас)
ε = 10⁻⁷ (чтобы не делить на 0)
```

**НАШ LEARNING RATE:** 0.001

**ЧТО ЭТО ЗНАЧИТ:**
```
На каждом шаге обучения:
вес_новый = вес_старый - 0.001 × (направление_улучшения)
```

Маленький lr = медленное, но стабильное обучение
Большой lr = быстрое, но может "проскочить" минимум

---

### Процесс обучения:

**ПАРАМЕТРЫ:**
```
Batch size = 32     ← обрабатываем 32 изображения за раз
Epochs = 100        ← максимум 100 проходов по всем данным
Learning rate = 0.001
```

**ЧТО ТАКОЕ ЭПОХА:**
```
1 эпоха = один полный проход по всем 1030 обучающим изображениям

Количество шагов в эпохе:
1030 изображений / 32 в batch = 32.2 ≈ 33 шага
```

**ЧТО ПРОИСХОДИТ НА КАЖДОМ ШАГЕ:**
```
1. Берем batch из 32 изображений
2. Прогоняем через сеть (forward pass)
3. Вычисляем loss
4. Вычисляем градиенты (backward pass)
5. Обновляем веса (Adam)
6. Повторяем для следующего batch
```

### Early Stopping:

**ПРОБЛЕМА:** Сеть может начать переобучаться (запоминать обучающую выборку)

**РЕШЕНИЕ:** Остановить обучение, когда validation loss перестает улучшаться

**НАШИ ПАРАМЕТРЫ:**
```
patience = 15  ← ждем 15 эпох без улучшения
monitor = 'val_loss'  ← следим за loss на validation
restore_best_weights = True  ← возвращаем лучшие веса
```

**КАК РАБОТАЕТ:**
```
Эпоха 1: val_loss = 2.50  ← новый минимум! Сохраняем веса
Эпоха 2: val_loss = 2.20  ← еще лучше! Обновляем
Эпоха 3: val_loss = 1.90  ← еще лучше!
...
Эпоха 25: val_loss = 0.80  ← лучший результат!
Эпоха 26: val_loss = 0.82  ← хуже, но ждем (patience=1/15)
Эпоха 27: val_loss = 0.85  ← хуже (patience=2/15)
...
Эпоха 40: val_loss = 1.10  ← хуже (patience=15/15)

СТОП! Возвращаем веса с эпохи 25
```

### ReduceLROnPlateau:

**ЧТО ДЕЛАЕТ:** Уменьшает learning rate, если обучение застряло

**ПАРАМЕТРЫ:**
```
factor = 0.5  ← уменьшаем lr в 2 раза
patience = 10  ← ждем 10 эпох без улучшения
min_lr = 1e-7  ← минимальный lr
```

**ПРИМЕР:**
```
Эпохи 1-20: lr = 0.001, loss улучшается
Эпохи 21-30: lr = 0.001, loss не улучшается
Эпоха 31: lr → 0.0005  ← уменьшили!
Эпохи 31-40: lr = 0.0005, loss снова улучшается
```

---

## 6️⃣ РЕЗУЛЬТАТЫ ОБУЧЕНИЯ

### Итоговая точность: **41.09%**

**ЧТО ЭТО ЗНАЧИТ:**
```
Из 258 тестовых изображений:
Правильно распознано: 106 изображений
Неправильно: 152 изображения

Accuracy = 106/258 = 0.4109 = 41.09%
```

**ЭТО ПЛОХО ИЛИ ХОРОШО?**

**Сравнение:**
```
Случайное угадывание (7 классов): 1/7 = 14.3%
Наш результат: 41.09%
→ В 2.9 раза лучше случайности! Но все равно плохо для практики.
```

### Confusion Matrix (Матрица ошибок):

**ПРИМЕР (упрощенный):**
```
           Предсказано:
          0    1    2    3    4    5    6
      ┌────┬────┬────┬────┬────┬────┬────┐
И   0 │ 65 │ 15 │ 10 │  5 │  3 │  5 │  3 │ ← из 106 фото Bush
С     ├────┼────┼────┼────┼────┼────┼────┤
Т   1 │ 20 │ 15 │  8 │  2 │  1 │  0 │  1 │ ← из 47 фото Powell
И     ├────┼────┼────┼────┼────┼────┼────┤
Н   2 │ 12 │  5 │ 10 │  1 │  0 │  0 │  1 │ ← из 29 фото Blair
Н     ├────┼────┼────┼────┼────┼────┼────┤
О   3 │  8 │  3 │  2 │  8 │  2 │  1 │  0 │ ← из 24 фото Rumsfeld
      ├────┼────┼────┼────┼────┼────┼────┤
    4 │  7 │  2 │  1 │  3 │  8 │  0 │  1 │ ← из 22 фото Schroeder
      ├────┼────┼────┼────┼────┼────┼────┤
    5 │  5 │  1 │  0 │  2 │  1 │  5 │  1 │ ← из 15 фото Sharon
      ├────┼────┼────┼────┼────┼────┼────┤
    6 │  6 │  0 │  1 │  1 │  0 │  1 │  5 │ ← из 14 фото Chavez
      └────┴────┴────┴────┴────┴────┴────┘

Диагональ (выделено) = правильные предсказания
Остальное = ошибки
```

**АНАЛИЗ:**
- Класс 0 (Bush): 65/106 = 61% правильно (лучший)
- Класс 1 (Powell): 15/47 = 32% правильно
- Класс 6 (Chavez): 5/14 = 36% правильно

Сеть лучше всего распознает Буша (больше всего обучающих данных!)

### Метрики по классам:

**Precision, Recall, F1-Score:**

```
         Precision  Recall  F1-Score  Support
Class 0    0.53     0.61     0.57      106
Class 1    0.37     0.32     0.34       47
Class 2    0.31     0.34     0.33       29
Class 3    0.36     0.33     0.35       24
Class 4    0.53     0.36     0.43       22
Class 5    0.42     0.33     0.37       15
Class 6    0.38     0.36     0.37       14

Macro avg  0.41     0.38     0.39      258
```

**ЧТО ЭТО ЗНАЧИТ (на примере класса 0):**

**Precision = 0.53:**
```
Из всех, кого сеть назвала "Bush", только 53% действительно Bush
(остальные 47% - ошибки)
```

**Recall = 0.61:**
```
Из всех настоящих фото Bush, сеть нашла 61%
(39% пропустила)
```

**F1-Score = 0.57:**
```
Гармоническое среднее Precision и Recall
Баланс между точностью и полнотой
```

### ROC-AUC:

**AUC (Area Under Curve):**
```
Класс 0: AUC = 0.XX  (вероятно ~0.75)
Класс 1: AUC = 0.XX  (вероятно ~0.65)
...
Macro Average: ~0.70
```

**ЧТО ЭТО ЗНАЧИТ:**
- AUC = 0.5: случайное угадывание
- AUC = 1.0: идеальный классификатор
- AUC = 0.70: умеренное качество

---

## 7️⃣ ПОЧЕМУ RNN ПОКАЗАЛА ПЛОХОЙ РЕЗУЛЬТАТ?

### Фундаментальная проблема:

**RNN создана для ПОСЛЕДОВАТЕЛЬНОСТЕЙ:**
- Текст: слово → слово → слово
- Аудио: звук → звук → звук
- Временные ряды: цена_t → цена_{t+1} → цена_{t+2}

**НО ИЗОБРАЖЕНИЕ - НЕ ПОСЛЕДОВАТЕЛЬНОСТЬ!**

Изображение = 2D структура с пространственными связями

### Конкретные проблемы:

**1. Потеря пространственной информации:**

```
Строка 1: ●●●○○○●●●  ← глаз (важные пиксели рядом горизонтально)
Строка 2: ●●●○○○●●●
Строка 3: ○○○●●●○○○  ← рот (важные пиксели рядом)

RNN обрабатывает:
Шаг 1: [●●●○○○●●●]  строка 1
Шаг 2: [●●●○○○●●●]  строка 2
Шаг 3: [○○○●●●○○○]  строка 3

Проблема: RNN видит связь между строками (вертикальную),
но НЕ ВИДИТ горизонтальную структуру ВНУТРИ строки!
```

**2. Ограниченное рецептивное поле:**

На каждом шаге RNN "видит" только одну строку (37 пикселей). Не видит полную картину лица!

**3. Неоптимальная архитектура для изображений:**

Для изображений важны:
- Локальные паттерны (края, текстуры)
- Иерархические признаки (края → части лица → лицо)
- Пространственная инвариантность (глаз может быть где угодно)

RNN НЕ оптимизирована для этого!

---

## 🎯 ИТОГОВЫЕ ВЫВОДЫ ЛАБЫ 2:

### Что узнали:

1. **RNN/LSTM** - мощная архитектура для последовательностей
2. **LSTM решает** проблему vanishing gradient через gates
3. **41.09% accuracy** - недостаточно для практического применения
4. **Причина:** несоответствие архитектуры природе данных

### Что дальше:

Нужна архитектура, СПЕЦИАЛЬНО разработанная для изображений → **CNN!**

---

## 📌 ГЛАВНОЕ, ЧТО НУЖНО ЗАПОМНИТЬ:

1. **LFW dataset**: 1288 фото, 7 персон, 50×37 пикселей
2. **Preprocessing**: нормализация + reshape для RNN + one-hot encoding
3. **LSTM**: 3 gates (forget, input, output) управляют памятью
4. **Архитектура**: 2 LSTM (128 нейронов) + Dropout + Dense
5. **Обучение**: Adam optimizer, categorical crossentropy, early stopping
6. **Результат**: 41.09% accuracy
7. **Проблема**: RNN не подходит для пространственных данных
8. **Confusion matrix**: показывает, какие классы путает сеть
9. **Метрики**: Accuracy, Precision, Recall, F1-Score, AUC

**RNN для изображений = использовать молоток для закручивания болтов!**

Завтра на экзамене сможешь объяснить, ПОЧЕМУ RNN не подходит для изображений! 💪
