# Лекция: Кластеризация данных - Теория и практика

## Содержание
1. [Что такое кластеризация?](#что-такое-кластеризация)
2. [Методы кластеризации](#методы-кластеризации)
3. [Метрики расстояния](#метрики-расстояния)
4. [Оценка качества кластеризации](#оценка-качества-кластеризации)
5. [ROC и AUC кривые](#roc-и-auc-кривые)
6. [Объяснение результатов лабораторной](#объяснение-результатов)

---

## Что такое кластеризация?

**Кластеризация** - это метод машинного обучения без учителя, который группирует похожие объекты вместе.

### Простая аналогия
Представьте, что у вас куча фотографий животных (кошки, собаки, птицы). Кластеризация автоматически разложит их по группам на основе похожести, даже если вы не сказали алгоритму, кто где.

### Зачем это нужно?
- **Сегментация клиентов**: группировка покупателей по поведению
- **Обнаружение аномалий**: поиск необычного трафика в сети (наша лаба!)
- **Сжатие данных**: объединение похожих элементов
- **Рекомендательные системы**: группировка похожих товаров/фильмов

---

## Методы кластеризации

### 1. K-means (К-средних)

**Суть**: Делим данные на K групп так, чтобы объекты внутри группы были похожи, а между группами - различались.

**Как работает:**
1. Выбираем K случайных точек как центры кластеров (центроиды)
2. Каждый объект относим к ближайшему центроиду
3. Пересчитываем центроиды как среднее всех объектов в кластере
4. Повторяем шаги 2-3, пока центроиды не перестанут двигаться

**Математика:**
Минимизируем сумму квадратов расстояний от объектов до их центроидов:

```
J = ∑∑ ||x - μₖ||²
```
где:
- x - объект
- μₖ - центроид кластера k
- ||...||² - квадрат евклидова расстояния

**Плюсы:**
- Быстрый
- Простой в понимании
- Хорош для больших данных

**Минусы:**
- Нужно заранее знать K (количество кластеров)
- Чувствителен к начальной инициализации
- Плохо работает с кластерами неправильной формы

**В нашей лабе:** Silhouette=0.176, AUC=0.433

---

### 2. K-means++ (Улучшенный K-means)

**Суть**: Та же математика, но умнее выбираем начальные центроиды.

**Отличие от обычного K-means:**
- **K-means**: случайно выбирает начальные центроиды → может попасть в плохую конфигурацию
- **K-means++**: выбирает центроиды так, чтобы они были дальше друг от друга → лучший старт

**Алгоритм инициализации:**
1. Выбираем первый центроид случайно
2. Для каждого следующего: выбираем точку с вероятностью, пропорциональной квадрату расстояния до ближайшего уже выбранного центроида
3. Повторяем, пока не выберем K центроидов

**Математика:**
Вероятность выбора точки x как нового центроида:

```
P(x) = D(x)² / ∑D(xᵢ)²
```
где D(x) - расстояние до ближайшего центроида

**Плюсы:**
- Все плюсы K-means
- Более стабильные результаты
- Меньше зависит от случайности

**В нашей лабе:** Silhouette=0.176, AUC=0.480 (лучше обычного K-means!)

---

### 3. Агломеративная кластеризация

**Суть**: Строим иерархию кластеров снизу вверх - сначала каждый объект сам по себе, потом постепенно объединяем ближайшие.

**Как работает:**
1. В начале каждый объект = отдельный кластер
2. Находим два самых близких кластера
3. Объединяем их в один
4. Повторяем шаги 2-3, пока не получим нужное количество кластеров

**Типы связи (linkage):**
- **Single (одиночная)**: расстояние между ближайшими точками кластеров
- **Complete (полная)**: расстояние между самыми дальними точками
- **Average (средняя)**: среднее расстояние между всеми парами точек (использовали в лабе)
- **Ward**: минимизирует дисперсию при объединении

**Математика (average linkage):**
```
d(A, B) = (1 / |A||B|) ∑∑ d(a, b)
         a∈A b∈B
```
где A и B - два кластера

**Плюсы:**
- Не нужно заранее знать K
- Создаёт дендрограмму (дерево кластеров)
- Работает с разными метриками расстояния
- Может найти кластеры сложной формы

**Минусы:**
- Медленнее K-means (O(n³) vs O(nKi))
- Требует больше памяти
- Решения необратимы (объединили - не разъединишь)

**В нашей лабе:**
- С евклидовым расстоянием: Silhouette=0.195, AUC=0.491
- С манхэттенским: Silhouette=0.185, AUC=0.498 (**лучший результат!**)
- С Чебышевым: Silhouette=0.129, AUC=0.476

---

## Метрики расстояния

Расстояние - это мера того, насколько два объекта различаются. Разные метрики считают это по-разному.

### 1. Евклидово расстояние (Euclidean)

**Суть**: Прямая линия между двумя точками (как измеряешь линейкой).

**Формула:**
```
d(x, y) = √[(x₁-y₁)² + (x₂-y₂)² + ... + (xₙ-yₙ)²]
```

**Пример 2D:**
Точки A(1,2) и B(4,6):
```
d = √[(4-1)² + (6-2)²] = √[9 + 16] = √25 = 5
```

**Когда использовать:**
- Универсальная метрика для большинства задач
- Когда все признаки примерно одинаково важны
- Для данных в декартовом пространстве

**Свойства:**
- Учитывает все направления одинаково
- Чувствительна к масштабу (поэтому нужна нормализация!)
- Чувствительна к выбросам (из-за квадрата)

---

### 2. Манхэттенское расстояние (Manhattan / L1)

**Суть**: Как идти по городским кварталам - только по горизонтали и вертикали, нельзя по диагонали.

**Формула:**
```
d(x, y) = |x₁-y₁| + |x₂-y₂| + ... + |xₙ-yₙ|
```

**Пример 2D:**
Точки A(1,2) и B(4,6):
```
d = |4-1| + |6-2| = 3 + 4 = 7
```

**Когда использовать:**
- Когда движение ограничено осями координат
- Данные с ортогональной структурой
- Когда нужна устойчивость к выбросам

**Свойства:**
- Менее чувствительна к выбросам (нет квадрата!)
- Больше подходит для высокоразмерных данных
- Часто дает лучшие результаты в ML

**В нашей лабе:** Показала **лучший AUC = 0.498**!

---

### 3. Расстояние Чебышева (Chebyshev / L∞)

**Суть**: Максимальное отличие по любой координате (как ход короля в шахматах).

**Формула:**
```
d(x, y) = max(|x₁-y₁|, |x₂-y₂|, ..., |xₙ-yₙ|)
```

**Пример 2D:**
Точки A(1,2) и B(4,6):
```
d = max(|4-1|, |6-2|) = max(3, 4) = 4
```

**Когда использовать:**
- Когда важна максимальная разница
- Анализ экстремальных значений
- Задачи с временными рядами
- Когда один признак критичен

**Свойства:**
- Учитывает только самую большую разницу
- Игнорирует малые различия по другим измерениям
- Полезна для обнаружения аномалий

---

### Сравнение метрик (визуально)

Для точек A(0,0) и B(3,4):
- **Евклидово**: 5.0 (гипотенуза треугольника)
- **Манхэттен**: 7.0 (идем вправо 3, вверх 4)
- **Чебышев**: 4.0 (максимум из 3 и 4)

**Какую выбрать?**
- Не знаешь - бери **евклидово**
- Высокая размерность или выбросы - **манхэттен**
- Важны экстремумы - **Чебышев**

---

## Оценка качества кластеризации

Как понять, хороша ли кластеризация? Используем метрики!

### 1. Silhouette Score (Силуэт)

**Суть**: Насколько объект хорошо вписывается в свой кластер и насколько он далек от других кластеров.

**Формула для одного объекта:**
```
s = (b - a) / max(a, b)
```
где:
- **a** = среднее расстояние до объектов в своём кластере
- **b** = среднее расстояние до объектов в ближайшем соседнем кластере

**Диапазон:** от -1 до +1

**Интерпретация:**
- **s ≈ +1**: объект хорошо вписывается в свой кластер, далеко от других ✅
- **s ≈ 0**: объект на границе между кластерами 😐
- **s ≈ -1**: объект, скорее всего, в неправильном кластере ❌

**Средний Silhouette Score** - среднее по всем объектам.

**В нашей лабе:**
- K-means: 0.176 (слабовато, кластеры перекрываются)
- Агломеративная (евклидова): 0.195 (чуть лучше)

**Что делать, если Silhouette низкий?**
- Попробовать другое K
- Попробовать другой метод кластеризации
- Другую метрику расстояния
- Возможно, данные плохо кластеризуются

---

### 2. Davies-Bouldin Index (Индекс Дэвиса-Болдина)

**Суть**: Отношение внутрикластерного расстояния к межкластерному. Чем **меньше**, тем лучше!

**Формула:**
```
DB = (1/K) ∑ max((σᵢ + σⱼ) / d(cᵢ, cⱼ))
      i≠j
```
где:
- σᵢ = среднее расстояние объектов кластера i до его центроида
- d(cᵢ, cⱼ) = расстояние между центроидами кластеров i и j
- K = количество кластеров

**Интерпретация:**
- **DB → 0**: кластеры компактные и далеко друг от друга ✅
- **DB → ∞**: кластеры растянутые или близко друг к другу ❌

**В нашей лабе:**
- K-means: 1.76 (средне)
- K-means++: 1.61 (лучше!)
- Агломеративная (евклидова): 0.93 (**лучший результат!**)

**Почему важно:**
Измеряет компактность (насколько плотные кластеры) и сепарабельность (насколько они разделены).

---

### 3. Calinski-Harabasz Score (Индекс Калински-Харабаша)

**Суть**: Отношение межкластерной дисперсии к внутрикластерной. Чем **больше**, тем лучше!

**Формула:**
```
CH = [SSB / (K-1)] / [SSW / (N-K)]
```
где:
- **SSB** = Between-cluster Sum of Squares (дисперсия между кластерами)
- **SSW** = Within-cluster Sum of Squares (дисперсия внутри кластеров)
- K = количество кластеров
- N = количество объектов

**Математика подробнее:**
```
SSB = ∑ nₖ × ||μₖ - μ||²  (насколько центроиды далеко от общего центра)
SSW = ∑∑ ||xᵢ - μₖ||²    (насколько объекты близко к своим центроидам)
```

**Интерпретация:**
- **Большое CH**: кластеры плотные внутри, далеко друг от друга ✅
- **Маленькое CH**: кластеры размазанные или близко друг к другу ❌

**В нашей лабе:**
- K-means: 11,246 (хорошо для большого датасета)
- K-means++: 12,891 (**лучше!**)
- Агломеративная: 110-190 (ниже, но это на меньшей выборке)

**Когда использовать:**
- Когда нужно быстро оценить качество
- Для сравнения разных K
- Работает быстрее Silhouette

---

### Как выбирать оптимальное K?

#### Метод локтя (Elbow Method)

Строим график зависимости Inertia (сумма квадратов расстояний) от K.

**Inertia** (инерция):
```
Inertia = ∑∑ ||x - μₖ||²
```

**Как читать график:**
- K растет → Inertia падает (больше кластеров = меньше ошибка)
- Ищем "локоть" - точку, где график резко меняет наклон
- После "локтя" улучшение замедляется

**В нашей лабе:**
- K=2: Inertia=168,074
- K=7: Inertia=106,664
- K=8: Inertia=102,631 ← **выбрали этот**

Silhouette также помогает: К=8 дал 0.177 (лучший среди проверенных).

---

## ROC и AUC кривые

Теперь о классификации! Мы применили кластеризацию для разделения нормального и аномального трафика.

### Матрица ошибок (Confusion Matrix)

```
                 Predicted
                 0       1
Actual  0       TN      FP
        1       FN      TP
```

где:
- **TP** (True Positive) = правильно определили как 1
- **TN** (True Negative) = правильно определили как 0
- **FP** (False Positive) = ошибочно определили как 1 (ложная тревога)
- **FN** (False Negative) = ошибочно определили как 0 (пропустили)

**Пример из лабы (K-means++):**
```
                Predicted
                0         1
Actual  0    35,625    5,159
        1    58,113    5,448
```

---

### Метрики на основе матрицы

**1. True Positive Rate (TPR) / Recall / Sensitivity:**
```
TPR = TP / (TP + FN)
```
Какую долю реальных 1 мы нашли?

**2. False Positive Rate (FPR):**
```
FPR = FP / (FP + TN)
```
Какую долю реальных 0 мы ошибочно назвали 1?

**3. Precision (Точность):**
```
Precision = TP / (TP + FP)
```
Из всех, кого мы назвали 1, сколько действительно 1?

---

### ROC кривая (Receiver Operating Characteristic)

**Суть**: График зависимости TPR от FPR при разных порогах классификации.

**Как строится:**
1. Меняем порог отсечения от 0 до 1
2. Для каждого порога считаем TPR и FPR
3. Строим график TPR(y) vs FPR(x)

**Идеальная ROC:**
- Идёт по левому верхнему углу
- TPR=1 (нашли все), FPR=0 (без ложных тревог)

**Плохая ROC:**
- Диагональ 45° (как случайное угадывание)

**В реальности:**
- ROC между идеалом и диагональю
- Чем ближе к левому верхнему углу, тем лучше

---

### AUC (Area Under Curve)

**Суть**: Площадь под ROC кривой. Одно число для оценки качества.

**Диапазон:** от 0 до 1

**Интерпретация:**
- **AUC = 1.0**: идеальная классификация ✅
- **AUC = 0.9-1.0**: отлично
- **AUC = 0.8-0.9**: хорошо
- **AUC = 0.7-0.8**: приемлемо
- **AUC = 0.6-0.7**: плохо
- **AUC = 0.5**: как случайное угадывание 😐
- **AUC < 0.5**: хуже случайного (что-то пошло не так) ❌

**Вероятностная интерпретация:**
AUC = вероятность того, что случайный положительный объект будет оценен выше, чем случайный отрицательный.

**В нашей лабе:**
- K-means: AUC=0.433 (ниже 0.5 - плохо!)
- K-means++: AUC=0.480 (лучше, но все равно слабо)
- Агломеративная (manhattan): AUC=0.498 (**лучший, почти случайный**)

**Почему AUC низкий?**
1. Кластеризация != классификация
2. Датасет может плохо разделяться на кластеры
3. Признаки могут быть недостаточно информативными
4. Нужны другие методы (обучение с учителем)

---

## Объяснение результатов лабораторной

Давайте разберем, что означает вывод программы построчно.

### 1. Загрузка данных

```
✓ Датасет загружен: 104345 строк, 23 столбцов
✓ Уникальные метки: {0: 63561, 1: 40784}
```

**Что это значит:**
- 104,345 записей о сетевом трафике
- 23 признака (пакеты, байты, IP, порты и т.д.)
- Две метки: 0 (нормальный трафик) и 1 (аномальный)
- Классы несбалансированы: 61% нормального, 39% аномального

---

### 2. Предобработка

```
✓ Признаков: 19, Размер: (104345, 19)
✓ PCA: объяснённая дисперсия = 0.223, 0.132
```

**Что это значит:**
- Из 23 столбцов взяли 19 числовых (исключили IP-адреса, протокол, метку)
- PCA сжал 19 признаков до 2 для визуализации
- Первая компонента объясняет 22.3% дисперсии данных
- Вторая компонента - еще 13.2%
- Вместе: 35.5% информации (неплохо для 2D!)

**PCA (Principal Component Analysis)** - метод уменьшения размерности:
- Находит главные направления изменчивости данных
- Проецирует данные на эти направления
- Теряется информация, но сохраняется основная структура

---

### 3. Определение K

```
✓ Вычисление метрик...
✓ Рекомендуемое количество кластеров: 8
```

**Что это значит:**
- Проверили K от 2 до 8
- K=8 дал лучший Silhouette Score
- Это не означает, что в данных ровно 8 групп!
- Это компромисс между простотой (мало K) и точностью (много K)

---

### 4. Результаты K-means

```
✓ K-means (евклидово расстояние):
  - Silhouette Score: 0.1765
  - Davies-Bouldin Index: 1.7596
  - Calinski-Harabasz Score: 11246.23
```

**Что это значит:**
- Silhouette=0.176 - кластеры слабо разделены, много перекрытий
- DB=1.76 - средняя компактность и разделимость
- CH=11246 - высокое значение (хорошо для размера данных)

**Общий вывод:** Кластеры есть, но границы размытые.

---

### 5. Результаты K-means++

```
✓ K-means++ (улучшенная инициализация):
  - Silhouette Score: 0.1762
  - Davies-Bouldin Index: 1.6087
  - Calinski-Harabasz Score: 12890.66
```

**Что это значит:**
- Silhouette похож на обычный K-means
- DB=1.61 - **лучше** (ниже = лучше)
- CH=12891 - **лучше** (выше = лучше)

**Общий вывод:** K-means++ немного превосходит обычный K-means благодаря лучшей инициализации.

---

### 6. Агломеративная кластеризация

```
→ Агломеративная кластеризация (euclidean)...
  - Silhouette Score: 0.1947
  - Davies-Bouldin Index: 0.9316
  - Calinski-Harabasz Score: 110.16
```

**Что это значит:**
- Silhouette=0.195 - **лучший среди всех** методов!
- DB=0.93 - **намного лучше** K-means (0.93 vs 1.76)
- CH=110 - низкий, но это артефакт (работали на меньшей выборке)

**С разными метриками:**
- Manhattan: Silhouette=0.185, AUC=0.498 (**лучший AUC!**)
- Chebyshev: Silhouette=0.129 (худший)

**Общий вывод:** Агломеративная кластеризация показала лучшие структурные метрики.

---

### 7. Бинарная классификация

```
✓ Распределение бинарных классов: [40784 63561]
```

**Что это значит:**
- Преобразовали многоклассовую задачу в бинарную
- Класс 0: 40,784 объекта (39%)
- Класс 1: 63,561 объект (61%)
- Несбалансированные классы - это норма для данных о трафике

---

### 8. ROC и AUC

```
✓ kmeans:
  - AUC Score: 0.4333
  - Матрица ошибок:
[[26779 14005]
 [50208 13353]]
```

**Что это значит:**

**Матрица ошибок:**
- TN=26,779: правильно определили как класс 0
- FP=14,005: ошибочно назвали 0 единицей
- FN=50,208: ошибочно назвали 1 нулем (много пропусков!)
- TP=13,353: правильно определили как класс 1

**Метрики:**
- Recall (TPR) = 13353/(13353+50208) = 21% (нашли только 21% аномалий!)
- Precision = 13353/(13353+14005) = 49% (половина тревог ложные)
- AUC = 0.433 < 0.5 (хуже случайного!)

**Почему так плохо?**
- Кластеризация не знает о метках, кластеризует по другим признакам
- Нормальный и аномальный трафик могут быть похожи по признакам
- Нужна классификация с учителем (SVM, Random Forest и т.д.)

**Лучший результат (manhattan):**
- AUC=0.498 ≈ 0.5 (почти как случайное угадывание)

---

### 9. Итоговое сравнение

```
                   Метод  Silhouette  Davies-Bouldin  Calinski-Harabasz    AUC
                 kmeans    0.176533        1.759581       11246.229849  0.433344
               kmeans++    0.176195        1.608658       12890.661269  0.479609
agglomerative_euclidean    0.194705        0.931617         110.157489  0.491361
agglomerative_manhattan    0.184647        1.022817         164.128826  0.497686
```

**Главные выводы:**

1. **Лучший по структуре кластеров:**
   - Agglomerative euclidean (Silhouette=0.195, DB=0.93)
   - Кластеры более компактные и разделенные

2. **Лучший по классификации:**
   - Agglomerative manhattan (AUC=0.498)
   - Но все равно близко к случайному!

3. **K-means++ vs K-means:**
   - K-means++ стабильнее (лучше DB и CH)
   - Практически одинаковый Silhouette

4. **Почему разные метрики дают разные результаты?**
   - Структурные метрики (Silhouette, DB, CH) оценивают геометрию кластеров
   - AUC оценивает соответствие кластеров реальным меткам
   - Они могут не совпадать!

---

## Что мы делали в лабораторной (пошагово)

### Требования задания

**1. Найти датасет** ✅
- Взяли датасет SDN (сетевой трафик)
- 104,345 записей, 23 признака

**2. Определить кластеры** ✅
- Использовали метод локтя
- Выбрали K=8 на основе Silhouette Score

**3. K-means** ✅
- Реализовали стандартный K-means
- Метрика: евклидово расстояние

**4. K-means++** ✅
- Реализовали улучшенную версию
- Лучшая инициализация центроидов

**5. Агломеративная кластеризация** ✅
- Реализовали hierarchical clustering
- Average linkage

**6. Три метрики расстояния** ✅
- Евклидово (Euclidean)
- Манхэттенское (Manhattan / L1)
- Чебышева (Chebyshev / L∞)

**7. Бинарная и небинарная классификация** ✅
- Бинарная: 0 vs 1 (нормальный vs аномальный)
- Небинарная: 8 кластеров

**8. ROC и AUC кривые** ✅
- Построили для всех методов
- Оценили качество классификации

**9. Выводы** ✅
- Сравнили все методы
- Определили лучший (agglomerative manhattan)

---

## Дополнительно (не требовалось, но сделали)

- **Метод локтя** для определения K
- **PCA** для визуализации в 2D
- **Дополнительные метрики** (Davies-Bouldin, Calinski-Harabasz)
- **Красивые визуализации** (4 графика)
- **Подробные выводы** в файл

---

## Ключевые выводы для понимания

### 1. Кластеризация ≠ Классификация
- **Кластеризация**: ищет структуру без меток (unsupervised)
- **Классификация**: учится по меткам (supervised)
- Поэтому наш AUC низкий - кластеризация не обучалась на метках!

### 2. Разные метрики - разные аспекты
- **Silhouette**: геометрия кластеров
- **Davies-Bouldin**: компактность и разделимость
- **Calinski-Harabasz**: дисперсия между/внутри
- **AUC**: соответствие реальным меткам

Метод может быть хорош по одной метрике и плох по другой!

### 3. Выбор метрики расстояния важен
- Евклидово - универсально
- Манхэттенское - для высокоразмерных данных и выбросов
- Чебышева - для экстремумов

В нашей лабе manhattan показал лучший AUC!

### 4. K-means++ > K-means
- Всегда используйте K-means++ вместо обычного K-means
- Лучшая инициализация → стабильнее результаты
- Почти та же скорость

### 5. Агломеративная кластеризация мощнее, но медленнее
- Лучше находит структуру (Silhouette, DB)
- Работает с любыми метриками расстояния
- Но требует O(n²) памяти и O(n³) времени

### 6. Для обнаружения аномалий кластеризация - не лучший выбор
- AUC ≈ 0.5 (случайное угадывание)
- Лучше использовать:
  - Isolation Forest
  - One-Class SVM
  - Autoencoder
  - Supervised методы (если есть метки)

---

## Рекомендации для защиты лабы

### Что спросят и что отвечать

**Q: Чем K-means отличается от K-means++?**
A: K-means++ умнее выбирает начальные центроиды - дальше друг от друга, что дает лучшую конвергенцию. K-means выбирает случайно.

**Q: Почему Silhouette Score низкий?**
A: Значение 0.17-0.19 означает, что кластеры перекрываются. Это нормально для сложных реальных данных. Данные о трафике могут не иметь четких границ.

**Q: Что означает AUC = 0.5?**
A: Классификатор работает как случайное угадывание. Это ожидаемо, т.к. кластеризация не обучалась на метках.

**Q: Какой метод лучший?**
A: Зависит от критерия:
- По структуре: агломеративная с euclidean (Silhouette=0.195)
- По классификации: агломеративная с manhattan (AUC=0.498)
- По скорости: K-means++ (быстрее агломеративной)

**Q: Зачем нужна стандартизация?**
A: Признаки имеют разные масштабы (пакеты: тысячи, байты: миллионы). Без стандартизации большие признаки доминируют в расстоянии.

**Q: Как выбрать K?**
A: Метод локтя + Silhouette Score. Ищем "локоть" на графике Inertia и максимум Silhouette.

---

## Полезные ресурсы

### Книги
- "Pattern Recognition and Machine Learning" - Bishop
- "The Elements of Statistical Learning" - Hastie, Tibshirani, Friedman
- "Data Mining: Concepts and Techniques" - Han, Kamber, Pei

### Онлайн
- scikit-learn documentation: https://scikit-learn.org/stable/modules/clustering.html
- Курс Andrew Ng на Coursera (Machine Learning)
- StatQuest (YouTube канал с отличными объяснениями)

### Для практики
- Kaggle datasets и competitions
- UCI Machine Learning Repository
- Google Dataset Search

---

## Заключение

Кластеризация - мощный инструмент для исследования данных и поиска скрытых паттернов. В этой лабораторной мы:

1. **Освоили три метода**: K-means, K-means++, агломеративная кластеризация
2. **Разобрались с метриками расстояния**: евклидово, манхэттенское, Чебышева
3. **Научились оценивать качество**: Silhouette, Davies-Bouldin, Calinski-Harabasz, ROC/AUC
4. **Применили на реальных данных**: датасет сетевого трафика SDN

Главный урок: **не существует универсально лучшего метода**. Выбор зависит от данных, задачи и критериев оценки. Пробуйте разные подходы и сравнивайте результаты!

---

*Удачи на защите! 🚀*
