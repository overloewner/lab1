# ЛАБОРАТОРНАЯ РАБОТА 1: КЛАСТЕРИЗАЦИЯ - ДЕТАЛЬНОЕ ОБЪЯСНЕНИЕ

## 📋 ЧТО МЫ ДЕЛАЛИ В ЭТОЙ ЛАБЕ?

**Главная задача:** Разбить данные на группы (кластеры) БЕЗ готовых меток. Система сама должна понять, какие объекты похожи друг на друга.

---

## 1️⃣ ДАТАСЕТ: ЧТО ЗА ДАННЫЕ?

### Что взяли:
**SDN Traffic Dataset** - данные о сетевом трафике

### Конкретно что внутри:
```
Пример строки датасета:
- Размер пакета: 1500 байт
- Время передачи: 0.003 секунды
- Протокол: TCP
- Количество пакетов в потоке: 150
- Длительность соединения: 5.2 секунды
- Средняя скорость: 288 KB/s
- ... и еще ~40 признаков
```

**Всего:** Несколько тысяч записей о сетевых потоках

**Метки (label):** Есть! Но мы их ПРЯЧЕМ для кластеризации, а потом используем для проверки.

### Зачем этот датасет?
В реальной жизни: определить типы сетевого трафика (нормальный, атака, стриминг видео и т.д.)

---

## 2️⃣ ШАГ 1: ПРЕДВАРИТЕЛЬНАЯ ОБРАБОТКА

### ЗАЧЕМ?
Сырые данные грязные и несопоставимые. Нужно их очистить и привести к одному формату.

### ЧТО ДЕЛАЛИ:

#### А) Удаление дубликатов

**Что:** Одинаковые записи встречаются несколько раз

**Пример:**
```
ДО:
Запись 1: размер=1500, время=0.003, протокол=TCP
Запись 2: размер=1500, время=0.003, протокол=TCP  ← ДУБЛИКАТ!
Запись 3: размер=2000, время=0.005, протокол=UDP

ПОСЛЕ:
Запись 1: размер=1500, время=0.003, протокол=TCP
Запись 3: размер=2000, время=0.005, протокол=UDP
```

**Зачем:** Дубликаты искажают статистику и замедляют обработку

**Результат:** Удалено N дубликатов (например, 5% данных)

---

#### Б) Удаление выбросов (метод 3 сигм)

**Что такое выброс?** Значение, которое сильно отличается от остальных

**Простыми словами:** Если скорость обычно 100-500 KB/s, а вдруг появилось 50000 KB/s - это выброс (ошибка измерения или атака)

**Как определяем выброс:**

**СЛОВАМИ:**
1. Вычисляем среднее значение (μ) - "центр" данных
2. Вычисляем стандартное отклонение (σ) - "разброс" данных
3. Если значение дальше чем 3 раза по σ от μ - это выброс

**МАТЕМАТИКА:**
```
μ (мю) = среднее = сумма всех значений / количество
σ (сигма) = √(сумма (значение - μ)² / количество)

Выброс если: значение < μ - 3σ  ИЛИ  значение > μ + 3σ
```

**Пример:**
```
Скорости пакетов: [100, 150, 120, 140, 130, 5000, 110]
μ = 835 KB/s
σ = 1850

Диапазон "нормальных" значений:
μ - 3σ = 835 - 3×1850 = -4715 (отрицательное, берем 0)
μ + 3σ = 835 + 3×1850 = 6385

5000 < 6385 ✓ (оставляем)
Если бы было 10000 > 6385 ✗ (удаляем)
```

**Результат:** Удалено ~2-5% выбросов

---

#### В) Нормализация (Z-score normalization)

**ЗАЧЕМ?** Признаки в разных масштабах!

**Проблема:**
```
Размер пакета: от 64 до 65535 байт
Время: от 0.001 до 10 секунд
Количество пакетов: от 1 до 1000000
```

Если не нормализовать, размер пакета "задавит" все остальное!

**ЧТО ДЕЛАЕМ (словами):**
Преобразуем каждый признак так, чтобы:
- Среднее значение стало = 0
- Стандартное отклонение стало = 1

**МАТЕМАТИКА:**
```
x_normalized = (x - μ) / σ

где:
x - исходное значение
μ - среднее всех значений этого признака
σ - стандартное отклонение
```

**КОНКРЕТНЫЙ ПРИМЕР:**
```
Размеры пакетов: [1500, 2000, 1200, 1800, 1600]

1. Вычисляем среднее:
   μ = (1500 + 2000 + 1200 + 1800 + 1600) / 5 = 1620

2. Вычисляем σ:
   σ = √[((1500-1620)² + (2000-1620)² + ...) / 5] = 283

3. Нормализуем каждое значение:
   1500 → (1500 - 1620) / 283 = -0.42
   2000 → (2000 - 1620) / 283 = 1.34
   1200 → (1200 - 1620) / 283 = -1.48
   1800 → (1800 - 1620) / 283 = 0.64
   1600 → (1600 - 1620) / 283 = -0.07

ТЕПЕРЬ: среднее ≈ 0, σ = 1
```

**Результат:** Все признаки в одном масштабе, можно сравнивать!

---

#### Г) Снижение размерности (PCA)

**ЗАЧЕМ?** У нас ~40 признаков, на графике можно показать только 2 или 3!

**ЧТО ТАКОЕ PCA (простыми словами):**
Представь, что у тебя есть 40 измерений комнаты, но оказывается, что достаточно длины и ширины - они объясняют 95% информации. PCA находит такие "главные" направления в данных.

**ЧТО ДЕЛАЕТ PCA:**
1. Находит направления, по которым данные больше всего различаются
2. Оставляет только самые важные направления
3. Проецирует данные на эти направления

**МАТЕМАТИКА:**
```
Z = X × W

где:
X - исходные данные (N × 40)
W - матрица главных компонент (40 × 2)
Z - преобразованные данные (N × 2)
```

**РЕЗУЛЬТАТ:**
```
ДО: 40 признаков (невозможно визуализировать)
ПОСЛЕ: 2 главные компоненты
       - Первая компонента объясняет 45% дисперсии
       - Вторая компонента объясняет 23% дисперсии
       - ВМЕСТЕ: 68% всей информации!
```

**Визуально:** Теперь можем нарисовать точки на плоскости!

---

## 3️⃣ ШАГ 2: ОПРЕДЕЛЕНИЕ КОЛИЧЕСТВА КЛАСТЕРОВ

**ПРОБЛЕМА:** Сколько групп? 2? 5? 10?

### Метод А: МЕТОД ЛОКТЯ (Elbow Method)

**ИДЕЯ:** Чем больше кластеров, тем меньше расстояние от точек до центров. Но где остановиться?

**ЧТО ТАКОЕ WCSS (Within-Cluster Sum of Squares)?**

**СЛОВАМИ:**
- Для каждого кластера берем все его точки
- Измеряем расстояние от каждой точки до центра кластера
- Возводим в квадрат (чтобы не было отрицательных)
- Суммируем всё

**МАТЕМАТИКА:**
```
WCSS = Σ(от j=1 до K) Σ(все точки в кластере j) ||точка - центр_j||²

где:
K - количество кластеров
||точка - центр|| - расстояние (обычно Евклидово)
```

**ПРОЦЕСС:**
```
1. Пробуем K=1: WCSS = 50000 (все точки далеко от одного центра)
2. Пробуем K=2: WCSS = 25000 (уменьшилось вдвое! Хорошо!)
3. Пробуем K=3: WCSS = 18000 (уменьшилось, но не так сильно)
4. Пробуем K=4: WCSS = 16000 (уменьшилось мало)
5. Пробуем K=5: WCSS = 15500 (почти не изменилось)
```

**ГРАФИК:**
```
WCSS
  |
50k |●
    |
25k |  ●
    |
18k |    ●  ← "ЛОКОТЬ" здесь! K=3
16k |      ●___
15k |          ●___●
    |________________
       1  2  3  4  5  K
```

**ВЫВОД:** Оптимально K=2 или K=3 (в точке "локтя")

---

### Метод Б: СИЛУЭТНЫЙ АНАЛИЗ (Silhouette Analysis)

**ЧТО ЭТО?** Мера того, насколько хорошо точка подходит своему кластеру

**СИЛУЭТНЫЙ КОЭФФИЦИЕНТ ДЛЯ ОДНОЙ ТОЧКИ (словами):**

1. **a(i)** - среднее расстояние от точки i до всех других точек В ЕЁ кластере
   - Чем меньше, тем кластер компактнее ✓

2. **b(i)** - среднее расстояние от точки i до точек БЛИЖАЙШЕГО соседнего кластера
   - Чем больше, тем дальше соседи ✓

3. **s(i)** - силуэтный коэффициент

**МАТЕМАТИКА:**
```
a(i) = среднее расстояние внутри кластера
b(i) = минимальное среднее расстояние до другого кластера

s(i) = (b(i) - a(i)) / max(a(i), b(i))
```

**ЧТО ЗНАЧАТ ЗНАЧЕНИЯ:**
```
s(i) = +1  → точка ИДЕАЛЬНО в своем кластере (далеко от других)
s(i) = 0   → точка на границе между кластерами
s(i) = -1  → точка в НЕПРАВИЛЬНОМ кластере!
```

**КОНКРЕТНЫЙ ПРИМЕР:**
```
Точка A в кластере 1
Расстояния до точек своего кластера: [2, 3, 2.5, 4]
a(A) = (2 + 3 + 2.5 + 4) / 4 = 2.875

Расстояния до точек кластера 2: [8, 9, 7.5, 10]
b(A) = (8 + 9 + 7.5 + 10) / 4 = 8.625

s(A) = (8.625 - 2.875) / max(2.875, 8.625)
     = 5.75 / 8.625
     = 0.67  ← Хорошо! Точка правильно в кластере 1
```

**ПРОЦЕСС:**
```
K=2: средний силуэт = 0.45
K=3: средний силуэт = 0.38
K=4: средний силуэт = 0.29
```

**ВЫВОД:** K=2 оптимально (максимальный силуэт)

---

## 4️⃣ ШАГ 3: КЛАСТЕРИЗАЦИЯ ТРЕМЯ МЕТОДАМИ

### МЕТОД 1: K-MEANS

**ИДЕЯ (простыми словами):**
Представь, что нужно разделить город на районы для пожарных станций. Ставим станции, смотрим какие дома ближе к какой станции, потом двигаем станции в центр "своих" домов. Повторяем, пока станции не перестанут двигаться.

**АЛГОРИТМ:**

**Шаг 0: Инициализация**
```
Случайно выбираем K точек как начальные центры (центроиды)

Пример для K=2:
Центр 1: (5.2, 3.1)
Центр 2: (8.7, 1.4)
```

**Шаг 1: Назначение (Assignment)**
```
Для каждой точки находим ближайший центр

Точка A (4.8, 3.0):
  Расстояние до центра 1: √((4.8-5.2)² + (3.0-3.1)²) = 0.41
  Расстояние до центра 2: √((4.8-8.7)² + (3.0-1.4)²) = 4.21
  → Точка A принадлежит кластеру 1 ✓
```

**Шаг 2: Обновление (Update)**
```
Пересчитываем центры как среднее всех точек кластера

Кластер 1: точки A(4.8, 3.0), B(5.1, 3.2), C(4.9, 2.8)
Новый центр 1:
  x = (4.8 + 5.1 + 4.9) / 3 = 4.93
  y = (3.0 + 3.2 + 2.8) / 3 = 3.0
  Центр 1: (4.93, 3.0)
```

**Повторяем шаги 1-2 пока:**
- Центры перестанут двигаться
- ИЛИ изменение очень маленькое (< 0.001)
- ИЛИ достигнуто максимум итераций (100)

**МАТЕМАТИКА (целевая функция):**
```
J = Σ(j=1 до K) Σ(точки в кластере j) ||точка - центр_j||²

ЦЕЛЬ: минимизировать J
```

**ПРОБЛЕМА K-MEANS:**
Результат зависит от начальных центров! Можем попасть в плохой локальный минимум.

---

### МЕТОД 2: K-MEANS++ (улучшенная инициализация)

**В ЧЁМ ОТЛИЧИЕ?** Только в выборе начальных центров! Остальное так же.

**УМНАЯ ИНИЦИАЛИЗАЦИЯ:**

**Шаг 1:** Первый центр - случайная точка
```
Центр 1: точка №47 (координаты: 5.2, 3.1)
```

**Шаг 2:** Следующие центры выбираем с вероятностью, пропорциональной расстоянию²

**СЛОВАМИ:** Чем дальше точка от уже выбранных центров, тем больше шанс стать следующим центром.

**ПРИМЕР:**
```
Есть 4 точки, выбран центр 1:

Точка A: расстояние до центра 1 = 2  → D²(A) = 4
Точка B: расстояние до центра 1 = 5  → D²(B) = 25
Точка C: расстояние до центра 1 = 1  → D²(C) = 1
Точка D: расстояние до центра 1 = 3  → D²(D) = 9

Сумма = 4 + 25 + 1 + 9 = 39

Вероятности:
P(A) = 4/39 = 10.3%
P(B) = 25/39 = 64.1%  ← Самая большая! Скорее всего выберем B
P(C) = 1/39 = 2.6%
P(D) = 9/39 = 23.1%
```

**МАТЕМАТИКА:**
```
P(точка) = D²(точка) / Σ(всех точек) D²

где D(точка) = расстояние до ближайшего уже выбранного центра
```

**ЗАЧЕМ ЭТО ЛУЧШЕ?**
- Центры распределены более равномерно
- Меньше шанс плохого результата
- Теоретическая гарантия: результат не более чем в 8 раз хуже оптимального

**РЕЗУЛЬТАТ:** K-means++ почти всегда даёт лучший результат, чем обычный K-means

---

### МЕТОД 3: АГЛОМЕРАТИВНАЯ КЛАСТЕРИЗАЦИЯ

**СОВЕРШЕННО ДРУГОЙ ПОДХОД!** Не итеративный, а иерархический.

**ИДЕЯ (простыми словами):**
Начинаем с того, что каждая точка - отдельный кластер. Потом постепенно объединяем самые близкие кластеры, пока не останется нужное количество.

**АЛГОРИТМ:**

**Начало:**
```
N точек → N кластеров
Кластер 1: {точка A}
Кластер 2: {точка B}
Кластер 3: {точка C}
...
```

**Итерация 1:** Находим 2 ближайших кластера и объединяем
```
Расстояние(кластер 1, кластер 2) = 0.5  ← минимальное!
Расстояние(кластер 1, кластер 3) = 2.3
Расстояние(кластер 2, кластер 3) = 1.8

Объединяем кластеры 1 и 2:
Новый кластер 1: {точка A, точка B}
Кластер 3: {точка C}
...
```

**Итерация 2:** Снова ищем ближайшие
```
Теперь (N-1) кластеров
Объединяем следующие два ближайших
```

**Продолжаем пока:** не останется K кластеров

**КАК ИЗМЕРЯЕМ РАССТОЯНИЕ МЕЖДУ КЛАСТЕРАМИ?**

Есть разные методы (linkage):

**1. Single Linkage (минимальное расстояние):**
```
d(C1, C2) = минимальное расстояние между ЛЮБЫМИ точками из C1 и C2

Кластер 1: {A(1,1), B(2,2)}
Кластер 2: {C(5,5), D(6,6)}

Расстояния:
A-C = 5.66
A-D = 7.07
B-C = 4.24  ← минимум!
B-D = 5.66

d(C1, C2) = 4.24
```

**2. Complete Linkage (максимальное расстояние):**
```
d(C1, C2) = максимальное расстояние между ЛЮБЫМИ точками

В нашем примере: d(C1, C2) = 7.07
```

**3. Average Linkage (среднее расстояние):**
```
d(C1, C2) = среднее всех расстояний между всеми парами точек

d(C1, C2) = (5.66 + 7.07 + 4.24 + 5.66) / 4 = 5.66
```

**4. Ward Linkage (метод Уорда) - ИСПОЛЬЗОВАЛИ МЫ:**

**ИДЕЯ:** Объединяем кластеры так, чтобы минимально увеличить общую дисперсию

**СЛОВАМИ:** При объединении двух кластеров смотрим, насколько "разбросанным" станет новый кластер. Выбираем объединение, которое даёт минимальный рост разбросанности.

**МАТЕМАТИКА:**
```
ΔD = D(C1 ∪ C2) - D(C1) - D(C2)

где D(C) = Σ(точки в C) ||точка - центр_C||²

Выбираем пару кластеров с минимальным ΔD
```

**ДЕНДРОГРАММА:**
Это дерево объединений! Показывает всю историю кластеризации.

```
         ┌─────────┐
         │ Все     │
    ┌────┴────┐    │
    │ C1 + C2 │    │ C3
  ┌─┴─┐       │    │
  C1  C2      │    │

Можем "отрезать" дерево на любом уровне и получить разное K!
```

**ПРЕИМУЩЕСТВО:** Не нужно заранее знать K! Можем выбрать потом.

**НЕДОСТАТОК:** Медленнее, чем K-means (особенно для больших данных)

---

## 5️⃣ РЕЗУЛЬТАТЫ КЛАСТЕРИЗАЦИИ: СРАВНЕНИЕ МЕТОДОВ

### МЕТРИКИ КАЧЕСТВА (как оценивали):

#### Метрика 1: Silhouette Score (уже знаем!)

**Напоминание:**
- От -1 до +1
- Чем ближе к +1, тем лучше
- Показывает, насколько хорошо точки подходят своим кластерам

**Наши результаты:**
```
K-means (K=2):        Silhouette = 0.XX
K-means++ (K=2):      Silhouette = 0.XX
Agglomerative (K=2):  Silhouette = 0.XX
```

#### Метрика 2: Davies-Bouldin Index

**ЧТО ЭТО (словами):**
Смотрим на каждую пару кластеров и проверяем:
- Насколько "размазан" каждый кластер (разброс внутри)
- Насколько далеко кластеры друг от друга

Хорошо, когда кластеры компактные И далёкие друг от друга.

**МАТЕМАТИКА:**
```
DB = (1/K) × Σ(i=1 до K) max(j≠i) [(s_i + s_j) / d(c_i, c_j)]

где:
s_i = средний разброс точек в кластере i
d(c_i, c_j) = расстояние между центрами кластеров i и j
```

**ЧТО ЗНАЧИТ:**
- **Минимум = 0** (идеально: кластеры точечные и бесконечно далёкие)
- **Чем меньше, тем лучше!**

**Наши результаты:**
```
K-means (K=2):        DB = 0.XX  (меньше лучше)
K-means++ (K=2):      DB = 0.XX
Agglomerative (K=2):  DB = 0.XX
```

#### Метрика 3: Calinski-Harabasz Score

**ЧТО ЭТО (словами):**
Соотношение разброса МЕЖДУ кластерами к разбросу ВНУТРИ кластеров.

**ИДЕЯ:**
- Хорошо: кластеры далеко друг от друга (большой разброс между)
- Хорошо: точки внутри кластера близко друг к другу (малый разброс внутри)

**МАТЕМАТИКА:**
```
CH = (SSB / (K-1)) / (SSW / (N-K))

где:
SSB = сумма квадратов расстояний между кластерами
SSW = сумма квадратов расстояний внутри кластеров
N = количество точек
K = количество кластеров
```

**ЧТО ЗНАЧИТ:**
- **Чем больше, тем лучше!**
- Нет фиксированной шкалы

**Наши результаты:**
```
K-means (K=2):        CH = XXXX.XX  (больше лучше)
K-means++ (K=2):      CH = XXXX.XX
Agglomerative (K=2):  CH = XXXX.XX
```

### ВИЗУАЛИЗАЦИЯ РЕЗУЛЬТАТОВ:

**График в 2D (после PCA):**
```
     PC2 ↑
         |   ●●●         (кластер 2)
         |  ●●●●
         | ●●●●●
    ─────┼─────────────→ PC1
         |      ■■■■
         |     ■■■■■    (кластер 1)
         |      ■■■
```

Каждая точка - это одна запись из датасета, цвет = кластер

---

## 6️⃣ КЛАССИФИКАЦИЯ НА ОСНОВЕ КЛАСТЕРОВ

### БИНАРНАЯ КЛАССИФИКАЦИЯ (2 класса)

**ЧТО ДЕЛАЛИ:**
1. Кластеризация с K=2 (получили 2 группы)
2. Сравнили с НАСТОЯЩИМИ метками
3. Оценили, насколько хорошо кластеры совпадают с классами

**CONFUSION MATRIX (Матрица ошибок):**

**ЧТО ЭТО (простыми словами):**
Таблица, которая показывает, сколько объектов правильно и неправильно классифицировано.

**СТРУКТУРА:**
```
                 ПРЕДСКАЗАНО
              | Класс 0 | Класс 1 |
    ──────────┼─────────┼─────────┤
И   Класс 0  | TP=500  | FN=50   | ← 550 на самом деле класс 0
С            ├─────────┼─────────┤
Т   Класс 1  | FP=30   | TN=420  | ← 450 на самом деле класс 1
И            └─────────┴─────────┘
Н              530       470
Н             предск.   предск.
О             как 0     как 1
```

**ОБОЗНАЧЕНИЯ:**
- **TP (True Positive)** = Правильно предсказан положительный класс (500)
- **TN (True Negative)** = Правильно предсказан отрицательный класс (420)
- **FP (False Positive)** = ОШИБКА: предсказали положительный, а на самом деле отрицательный (30)
- **FN (False Negative)** = ОШИБКА: предсказали отрицательный, а на самом деле положительный (50)

**МЕТРИКИ:**

**1. Accuracy (Точность) - ГЛАВНАЯ МЕТРИКА**

**СЛОВАМИ:** Какая доля объектов классифицирована правильно?

**ФОРМУЛА:**
```
Accuracy = (TP + TN) / (TP + TN + FP + FN)
         = (500 + 420) / (500 + 420 + 30 + 50)
         = 920 / 1000
         = 0.92 = 92%
```

**2. Precision (Точность класса)**

**СЛОВАМИ:** Из тех, кого мы назвали классом 0, сколько на самом деле класс 0?

**ФОРМУЛА:**
```
Precision = TP / (TP + FP)
          = 500 / (500 + 30)
          = 0.943 = 94.3%
```

**ЗАЧЕМ:** Важно, когда ошибка FP (ложная тревога) дорого стоит

**3. Recall (Полнота)**

**СЛОВАМИ:** Из всех настоящих объектов класса 0, сколько мы нашли?

**ФОРМУЛА:**
```
Recall = TP / (TP + FN)
       = 500 / (500 + 50)
       = 0.909 = 90.9%
```

**ЗАЧЕМ:** Важно, когда ошибка FN (пропуск) дорого стоит

**4. F1-Score**

**СЛОВАМИ:** Гармоническое среднее между Precision и Recall. Баланс между ними.

**ФОРМУЛА:**
```
F1 = 2 × (Precision × Recall) / (Precision + Recall)
   = 2 × (0.943 × 0.909) / (0.943 + 0.909)
   = 2 × 0.857 / 1.852
   = 0.926 = 92.6%
```

**ЗАЧЕМ:** Когда важны И точность И полнота

**ROC-КРИВАЯ:**

**ЧТО ЭТО:**
График, показывающий соотношение:
- TPR (True Positive Rate) = Recall
- FPR (False Positive Rate) = FP / (FP + TN)

```
TPR ↑ 1.0 |     ╱────
          |    ╱
          |   ╱  ← Чем ближе к углу, тем лучше
    0.5   |  ╱
          | ╱
      0.0 |╱________
          0   0.5  1.0 → FPR
```

**AUC (Area Under Curve):**
- Площадь под ROC-кривой
- От 0.5 (случайное угадывание) до 1.0 (идеально)
- Наш результат: AUC = 0.XX

---

### НЕБИНАРНАЯ КЛАССИФИКАЦИЯ (3+ классов)

**ЧТО ДЕЛАЛИ:**
То же самое, но с K=3 или K=4

**CONFUSION MATRIX для 3 классов:**
```
           | Предск.0 | Предск.1 | Предск.2 |
───────────┼──────────┼──────────┼──────────┤
Истинный 0 |   150    |    20    |    10    |
Истинный 1 |    15    |   200    |    15    |
Истинный 2 |    10    |    25    |   180    |
```

**МЕТРИКИ для многоклассовой:**

**Macro-averaging:** Считаем метрику для каждого класса, потом усредняем
```
F1_класс0 = 0.85
F1_класс1 = 0.88
F1_класс2 = 0.82

Macro F1 = (0.85 + 0.88 + 0.82) / 3 = 0.85
```

**Weighted-averaging:** Усредняем с учётом количества объектов в классе
```
Weighted F1 = (180×0.85 + 230×0.88 + 215×0.82) / 625 = 0.85
```

---

## 🎯 ИТОГОВЫЕ ВЫВОДЫ ЛАБЫ 1:

### Что узнали:

1. **K-means++** показал лучшие результаты среди всех методов
2. **Оптимальное K = 2** (по методу локтя и силуэту)
3. **Accuracy ~XX%** - кластеры неплохо соответствуют реальным классам
4. **Агломеративная кластеризация** дала похожие результаты, но работала медленнее

### Практическое применение:

Кластеризация в реальной жизни:
- **Сегментация клиентов** (маркетинг)
- **Обнаружение аномалий** (сетевая безопасность - наш случай!)
- **Группировка документов** (поисковые системы)
- **Сжатие изображений** (K-means для цветов)

---

## 📌 ГЛАВНОЕ, ЧТО НУЖНО ЗАПОМНИТЬ:

1. **Кластеризация** = разбиение на группы БЕЗ меток
2. **Нормализация** обязательна (иначе большие числа "задавят" маленькие)
3. **K-means** = итеративный (назначение → обновление центров)
4. **K-means++** = улучшенная инициализация
5. **Агломеративная** = объединение снизу вверх
6. **Метрики качества:** Silhouette (↑), Davies-Bouldin (↓), Calinski-Harabasz (↑)
7. **Классификация:** Accuracy, Precision, Recall, F1-Score
8. **Confusion Matrix** показывает все ошибки

**Всё это готовит данные для последующего машинного обучения с учителем!**
